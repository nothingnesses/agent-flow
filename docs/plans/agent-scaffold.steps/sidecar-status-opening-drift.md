### `sidecar-status-opening-drift`: DELETE the duplicated status label from every selected step sidecar whose opening strips to prose, and HAND THE REST to a successor step (`Q-78-statusdrift`, `Q-78-statusform`, `Q-78-statuswiden`, `Q-78-driftsplit`, `Q-78-driftsize`)

THE GOVERNING FACT, WHICH AN EARLIER DRAFT OF THIS STEP NEVER NAMED. `docs/plans/agent-scaffold.documentation-protocol.md:5`, rendered at `docs/plans/agent-scaffold.md:26`, ends: "The Step Details below carry each step's design, decisions, and (once done) outcome and evidence, and do not repeat the status label." So the opening state token is not merely stale in the files that contradict their status, it is text this plan's own protocol says must not be there. An earlier draft directed the implementer to write a corrected label into all of them, which would have authored fresh violations of that sentence, and it routed the question to `Q-78` as undecided when for this plan the protocol had already decided it.

THIS PROJECT HAS RULED THE DUPLICATION A DEFECT TWICE, AND THE SECOND RULING WAS WAITING FOR EXACTLY THIS STEP. `docs/plans/agent-scaffold.ledger.md:46` records round 2 finding G1, a bundle including "repeats `next` status label", and `:62` records the triager ruling it VALID at `high`. `:99` records round 3 finding L1, "`state-schema` repeats its status label", and `:101` records all eight findings of that round valid, with L1 alone left unfixed for one stated reason: "the leading status-word is a plan-wide convention, do not single out `state-schema` (leave it)". L1 WAS RAISED WHILE THE LABEL WAS ACCURATE, since `state-schema` was `deferred` and its opening said "Deferred", and it was still valid. So the DUPLICATION is the defect, independent of whether the copy currently happens to be right, and writing accurate labels would not close the class.

THE SCOPE WIDENED ON 2026-08-21 AND THIS STEP NOW CLOSES THE CLASS. An earlier scope stopped at the openings that CONTRADICT their step's declared `status` and left untouched every opening whose label happened to agree. The human widened it (`type:"decision"` `q_id:"Q-78-statuswiden"` in `docs/metrics/workflow.jsonl`), over holding at the contradicting subset, on the ground L1 already established and on the measured ground that an agreeing label is exactly what a contradicting one was before its step moved on. So the widened set is every opening that carries a status LABEL, and after this step and its successor together, `docs/plans/agent-scaffold.md:26` is TRUE of every sidecar the selector reaches. THIS STEP ALONE DOES NOT CLOSE THE CLASS, because the human split the authored replacements out of it on the same day. Two classes the selector does not reach are named exclusions under NOT IN SCOPE below, and a third, the openings whose leading word is an ordinary adjective, is excluded at selection and printed by criterion 1's complement command.

ONE FILE SET, SELECTED BY A COMMAND RATHER THAN BY A READING. ONE OBLIGATION, WHICH IS A READING. A step's `status` lives in `docs/plans/agent-scaffold.plan.toml` and is the single source of truth for it. Many step sidecars ALSO open by naming a state in prose, and that second copy was never maintained. EVERY SIDECAR WHOSE OPENING CARRIES A STATUS TOKEN IS THE SCOPE, and criterion 1 selects them so the set cannot be argued about. Inside each, the token GOES and the obligation is the whole opening LINE, which takes a reading rather than a strip, for reasons measured under WHAT THE FIX IS below. It changes nothing else about any step. A SUBSET NEEDS AN AUTHORED REPLACEMENT RATHER THAN A DELETION, and that subset is not this step's: the human split it out on 2026-08-21 and criterion 2 captures it as the HANDOVER LIST.

TWO HUMAN DECISIONS SET THIS STEP UP AND A THIRD WIDENED IT, kept apart because an earlier draft conflated the first two. `Q-78-statusdrift` (human, 2026-08-18) decided the TIMING AND ROUTE: fix this now as its own step, over folding it into the `Q-78` design pass and over flipping the affected statuses to `not-started`. Its receipt names those three options and no others, so it chose no FORM, and an earlier draft wrongly attributed "state the actual state" to it. Its `type:"decision"` receipt is in `docs/metrics/workflow.jsonl` with `q_id:"Q-78-statusdrift"`, carried by commit `e8b2992`.

`Q-78-statusform` (human, 2026-08-18) decided the FORM: DELETE the state token rather than replace it, and record the residual as a named exclusion. Chosen over keeping the label with the protocol sentence recorded as an exclusion, over keeping it and routing the conflict to `Q-78` item (g), and over cutting this step's justification prose down to what the criteria need. THE REASONING ACCEPTED, cited by name: Principle 8, Structured data first, project for humans, which names plan status first among the data that must have one structured home with the human views derived by projection, and which directs that the best long-term design beats minimal blast radius at this pre-adoption stage; and Principle 1, Prefer the cleaner long-term architecture over the smallest diff. A replacement restores hand-maintained copies of a field the TOML owns, which is the mechanism that produced this drift in the first place. Its `type:"decision"` receipt is in `docs/metrics/workflow.jsonl` with `q_id:"Q-78-statusform"`, carried by commit `5ca89bb`, and its four options and its `chosen` are the four recited here. Its `task` is `step-intent-fold` where `Q-78-statusdrift`'s is `step-intent-encoding`, because the two were recorded in different loops.

`Q-78-statuswiden` (human, 2026-08-21) decided the SCOPE, as the paragraph above records. It leaves the FORM exactly as `Q-78-statusform` set it: the widened files get the same deletion, not a corrected label.

`Q-78-driftsplit` (human, 2026-08-21) SPLIT THE STEP IN TWO. The mechanical deletions stay here. The files whose opening needs an AUTHORED replacement become their own step, measured before that step starts. Its `type:"decision"` receipt is in `docs/metrics/workflow.jsonl` with `q_id:"Q-78-driftsplit"`, and `docs/plans/agent-scaffold.ledger.md` records it as decision (9) of 2026-08-21.

THE MEASUREMENT THE SPLIT DIRECTED IS NOW MADE, AND IT MOVES THE FIGURE THE DECISION NAMED. The decision was taken against 28 files. Reproduce the three numbers with the commands under ACCEPTANCE below, on a throwaway copy of `docs/`:

- Criterion 1's UNANCHORED selector reaches 45 sidecars, and a bare token strip across all 45 leaves 28 that criterion 12 flags. That is the 28 the decision named, and it reproduces.
- NINE OF THOSE 45 ARE NOT IN THIS STEP'S SCOPE AT ALL. Their leading word is an ordinary adjective and not a repeated status label: `checks-kind-skip`, `classification-trivial-rename`, `drift-guard-optionality`, `instrument-magic-filename`, `module-spec-description-dead` and `se-principle-namespace` open "Deferred cleanup from the `Q-44` audit", `sidecar-ref-empty-string` opens "Deferred cleanup (issue I2-2", `sidecar-ref-symlink` opens "Deferred hardening (issue L1)." and `repoint-resume-prompts` opens "Deferred pack-prose follow-up." All nine are declared `status = "deferred"`, verified one by one, and in all nine the word names what the step IS. A bare strip leaves each of them ungrammatical, which is why criterion 12 flagged every one, and the fix rule would have rewritten nine correct sentences to satisfy a criterion whose own scope excludes them.
- Criterion 1's ANCHORED selector, which is the form below, reaches 36. A bare token strip across those 36 leaves NINETEEN that criterion 12 flags. THE AUTHORED-REPLACEMENT POPULATION IS NINETEEN.

THE CORRECTED FIGURE WENT BACK TO THE HUMAN AND THE SPLIT STANDS AT 19, receipt `type:"decision"` `q_id:"Q-78-driftsize"` (2026-08-21). A size the human weighed had moved, so the decision was re-put rather than adjusted here.

The two openings that a crude adjectival test also catches were checked and are genuine label uses: `state-schema` opens "Deferred and non-blocking" and `include-all-visible` opens "Skipped by decision". Both stay in scope, which is why the anchor admits a conjunction and a preposition after the token.

WHERE THE SPLIT CUTS, AND THE ONE PLACE THIS SIDECAR DEPARTS FROM THE DECISION'S LITERAL WORDING. The decision reads that the mechanical deletions proceed across the whole selected set and that the files needing an authored opening become their own step. Taken literally, this step would delete the token in all 36 and leave 19 openings reading as fragments in the published plan until the successor step lands. THIS SIDECAR CUTS THE OTHER WAY: the 19 leave this step WITH THEIR TOKEN STILL IN PLACE, and the successor step does the deletion and the authoring together, in one edit per file. THE GROUND: `render` inlines every one of those openings into `docs/plans/agent-scaffold.md`, so the literal reading ships 19 fragments into every reader's copy of the plan for the length of a whole review loop. Principle 3, Safe on existing projects, and Principle 1, Prefer the cleaner long-term architecture over the smallest diff, both argue against an intermediate state that no principle argues for, and Principle 5, Make illegal states unrepresentable, argues against admitting it at all. THE CONSEQUENCE FOR THE CRITERIA, so the departure is visible rather than buried: criterion 1's output is NOT empty for this step, it is exactly the 19 rows the successor step owns, and criterion 12 stays an empty-output criterion over the 17 files this step does fix. THE PLANNER ROUTED THIS READING TO THE HUMAN RATHER THAN SETTLE IT, AND THE HUMAN SETTLED IT ON 2026-08-21. The human chose this reading, over the literal one, and accepted the consequence that criterion 1 prints the handover list rather than nothing. The receipt is `type:"decision"` `q_id:"Q-78-drifthandover"` in `docs/metrics/workflow.jsonl`, and `docs/plans/agent-scaffold.ledger.md` records it as the thirteenth decision of that day. So criterion 1 below states a decided shape and not an open one.

THE STEP'S TITLE STATES THE SAME BOUNDARY, AND THE TITLE IS A HAND-DUPLICATED PAIR WITH NO CROSS-CHECK. `[[step]].title` in `docs/plans/agent-scaffold.plan.toml` and this sidecar's own first line carry the same sentence, and `render` reads NEITHER into the Roadmap table, which emits `| Step | Status | Notes |` and no title column, nor into the Step Details heading, which is this sidecar's first line inlined verbatim (`step_details_section`, `src/plan/render.rs`, discards the `Step` and keeps the body). MEASURED: `grep -n '^| Step' docs/plans/agent-scaffold.md` prints the three-column header, and the Step Details heading for this step is byte-identical to this file's first line. So a wrong title is wrong in two hand-maintained places and in no generated one, which is the duplication class this step exists to remove, one level up. An earlier round reported the Roadmap row as a projection of the title, and that is false.

### INCREMENT AND RISK CLASS

ONE INCREMENT, `sidecar-status-opening-drift-inc1`, DECLARED IN THE PLAN TOML AS A `[[step.increment]]` ENTRY WITH ITS CLASS, so a round record joins to it structurally rather than by a lexical prefix (Principle 8, Structured data first). It is `risky`, and it needs TWO consecutive clean review rounds.

THE GROUND, against the `AGENTS.md` test that an artefact is risky when a defect in it would be costly or hard to reverse. IT IS STATED AS A PREMISE AND A CONSEQUENCE, AND EACH HALF NAMES THE CRITERION THAT REFUSES IT. A ground whose consequence holds for a second reason is a ground no criterion tests, which is the defect this pass measured across two rounds.

THE PREMISE. The increment DELETES the leading status token from the opening LINE of every file on THIS STEP'S WORKLIST, authors no replacement line, and preserves the captured start of the prose that followed the token, because `Q-78-statusform` decided a deletion rather than a substitution. Any stale clauses later in that same line are removed by deletion rather than replaced with new prose. The handover files keep their token and this step does not open them.

THE CONSEQUENCE. `render` inlines every one of those opening lines verbatim into `docs/plans/agent-scaffold.md`, so every edit ships into the published plan.

BOTH HALVES ARE REFUSED BY CONSTRUCTION. Criterion 6 captures the pre-edit post-token opening anchor, requires that anchor to remain at the start of every worklist opening, and rejects every added word in those lines. The generic replacement that rewrites every opening therefore fails every anchor, and the probe-first construction that deletes a whole opening line fails the anchor for each line it destroys, even though both constructions satisfy the selector, path and numstat checks. Criterion 7 refuses the other half: an implementation that makes the permitted deletions and leaves `docs/plans/agent-scaffold.md` unregenerated exits 1 and names the first differing line. The premise and consequence now each have an executable oracle rather than a statement that the earlier criteria did not carry.

WHAT DECIDES THE CLASS IS THE READING OBLIGATION, which criteria 11 and 12 both state in their own text. The oracle for the corrected line is a reading and not a command. Nothing here catches an opening that still states a false claim after the deletion. The increment ships nothing to a scaffolded project and one revert undoes it, which argues the other way, and the reading decides it. NO FILE COUNT IS WRITTEN INTO THIS GROUND, because the worklist moves as sidecars are edited, and criterion 2 captures it by command rather than by a number. THE HUMAN RE-CONFIRMED THE CLASS ON 2026-08-21, over a drop to `low_risk`, receipt `type:"decision"` `q_id:"Q-78-driftclass"` in `docs/metrics/workflow.jsonl`. THE REASONING ACCEPTED, cited by name: Principle 3, Safe on existing projects, because every corrected line ships into the published plan and the only oracle for it is criterion 11's reading. AN EARLIER FORM OF THIS GROUND COUNTED 36 REWRITTEN SIDECARS, DESCRIBED A REPLACEMENT, AND DECIDED THE CLASS ON A READING OVER 36 FILES. All three describe the increment the paragraph at WHERE THE SPLIT CUTS refuses to build, and the criteria admit none of them. The classification matches `plan-order-array-position` increment 2, which is `risky` for a prose sweep of the same kind.

THE CLASS IS RECORDED HERE AT AUTHORING TIME because the plan's convention is that the declared class IS the loop-open classification (`AGENTS.md:56`), and a class asserted without a ground is the defect this pass polices elsewhere (Principle 6, Ground decisions in evidence). AN EARLIER DRAFT DECLARED `increment = []` AND STATED NO CLASS, so the step would have opened a loop with no recorded class and therefore no required clean-round count.

WHAT THE CRITERIA NEVER SETTLED, MEASURED, so the ruling is not mistaken for something the step already implied. Applying a pure token deletion to a contradicting opening in a throwaway copy and running criterion 1 gives ZERO rows for that file, exactly as a replacement did under the earlier narrower selector. Only the prose ever chose, which is why the choice had to go to the human. Under the WIDENED selector a replacement no longer passes criterion 1 at all, because the widened selector reports any token rather than a mismatched one.

WHY NO RECEIPT IS IN `[step.provenance].decisions`, RECORDED SO THE ABSENCE IS A CHOICE. `decisions` resolves fail-closed to a registered `[[question]]` id of `Q-<n>` shape: the provenance arm of `validate_source` (`src/plan/source.rs`) rejects a decision that `question_id_index` (`src/metrics.rs:427`) cannot parse, and then rejects one that names no `[[question]]`. `Q-78-statusdrift`, `Q-78-statusform` and `Q-78-statuswiden` are all sub-decision ids with a receipt and no registered `[[question]]`, the same convention `Q-55-<suffix>`, `Q-65-<suffix>` and `Q-77-changelogfix` already follow, so putting any of them there would fail `validate --source` on both arms. The step's provenance therefore names commits `e8b2992` and `5ca89bb`, which is where the first two receipts live, and the paragraphs above are the pointers to the ids. Registering a fresh `[[question]]` for them instead was considered and rejected: W4 joins a decided item to a receipt on `d.q_id == question.id` (`w4_problems`, `src/workflow.rs:309`), so a newly registered `Q-<n>` would demand a receipt with that exact id, which does not exist, and writing one is not this step's to do.

### THE DRIFT, MEASURED

THE METHOD, so the set is reproducible rather than asserted. For each Roadmap step, take the first line of `docs/plans/agent-scaffold.steps/<slug>.md` that is neither blank nor a Markdown heading, and read its leading word or phrase. When that leading token is one of the seven Roadmap status labels (`not started`, `in progress`, `complete`, `skipped`, `next`, `optional`, `deferred`), the file is in scope. This step's own sidecar carries no such token. THIS TEST SELECTS THE FILES AND DOES NOT DEFINE THE FIX: it reads the leading token only, while the obligation under WHAT THE FIX IS covers the whole opening line. Keeping the two apart is deliberate, because a selector has to be mechanical and the obligation cannot be.

THE COMPATIBLE-PAIR EXEMPTION IS RETIRED BY THE WIDENING AND IS RECORDED HERE AS HISTORY. Under the earlier scope, `optional` and `deferred` said nothing about whether work had started, so an opening of "Not started" alongside either added information without contradicting it, and four sidecars were exempt on that ground: `git-url-fetch` (`deferred`, opening "Not started (deferred).") and `greenfield-flake`, `later-enhancements` and `tui-authoring` (all `optional`). Under the widened scope the exemption buys nothing, because a compatible token is still a second copy of a field the TOML owns and it goes with the rest. The four files are in scope and the rule that once excused them is not applied.

THE OPENINGS THAT CONTRADICT THEIR DECLARED `status` ARE ENUMERATED BY SLUG, because that set carries a further obligation and because a fixed list of slugs does not expire while a count does. Ten `complete` steps open "Not started": `optional-modules`, `instrument-flag`, `reviewer-harness-field`, `reviewer-diversity`, `review-mode`, `doc-redundancy-cleanup`, `doc-currency-guidance`, `human-input-gate-reinforce`, `planner-folds-decisions` and `prompt-drift-guard`. Seven `complete` steps open "Next": `session-preflight`, `round-log-core`, `workflow-invariants`, `decision-receipt`, `structured-skeleton`, `reviewer-reproducible-evidence` and `decision-folder-currency`. Two `complete` steps open "Deferred": `state-schema` and `checks-runner-worktree-name-collision`. Two of the three `in-progress` steps open "Not started": `workflow-calibration` and `workflow-driver`. Every other file criterion 1 selects carries a token that agrees with, or is compatible with, its declared `status`, and each of those gets the same deletion with no authored replacement owed unless criterion 12 flags it.

THE THIRD `in-progress` STEP IS CLEAN AND IS NAMED SO THE READER KNOWS IT WAS CHECKED. `code-value-audit-static` opens "Build the Tier-0 slice of the Q-52 code-value audit", which is a statement of what the step does and not a claim about its state, so it carries no token and criterion 1 does not select it. THE SIDECARS THIS STEP DOES NOT TOUCH ARE EXACTLY THOSE WHOSE OPENING CARRIES NO STATUS TOKEN. No count of them is written here, and no count of the token-carrying set is written here either, because both move as the plan grows: an earlier form of this paragraph gave a complement and a total, and a later planning pass that added two steps falsified both without touching this file. What does not move is the enumeration above, which is a list of slugs.

### WHY THE STATUS IS RIGHT AND THE PROSE IS STALE, FOR THE TWO IN-PROGRESS CASES

This step corrects the PROSE, so the direction has to be established rather than assumed. It is established independently for each of the two, and neither derivation depends on the other.

`workflow-calibration` CONTRADICTS ITSELF INSIDE ONE FILE, which is the strongest form the evidence takes here and needs no external comparison. It opens "Not started (deferred)." and three paragraphs later states "WHERE THIS STEP'S AUDIT RECORDS LIVE (`Q-73`, human, 2026-08-13, already executed at `572e331`)", describing the 2026-08-13 audit as this step's delivered work. Outside the file: `docs/plans/workflow-calibration.explorations/` holds 12 files, and `docs/metrics/workflow.jsonl` carries two `type:"decision"` receipts whose `task` is `workflow-calibration`, `Q-73` (2026-08-13) and `Q-76-causation` (2026-08-14). Work has happened, so `in-progress` is right.

`workflow-driver` IS STALE ON TWO INDEPENDENTLY CHECKABLE COUNTS IN ONE SENTENCE. It opens "Not started; the build-start is a separate pending human decision, so this umbrella step stays `not-started` with no increments declared yet". First, the step declares THREE increments in the plan TOML, `workflow-driver-stage0a`, `workflow-driver-stage0b` and `workflow-driver-stage1`, so "no increments declared yet" is false against the same document the sentence sits beside. Second, `docs/metrics/workflow.jsonl` carries nine `type:"round"` records whose `task` is `workflow-driver`, spread across those three increments and dated 2026-07-19 and 07-20, plus three `type:"decision"` receipts (`Q-51` once, `Q-58` twice), and `docs/plans/workflow-driver-stage1.build-plan.md` and `docs/plans/mealy-workflow-driver.explorations/` both exist. Work has happened, so `in-progress` is right.

THE OTHER CONTRADICTING OPENINGS NEED NO SUCH DERIVATION, because they are `complete` steps and a `complete` step is by definition not "Not started", "Next" or "Deferred". One of them is close to self-correcting already and is called out so the implementer does not read it as a rewrite: `structured-skeleton` opens "Next; the planning pass is DONE (2026-07-18) and this umbrella step is built in six reviewed increments", so the same sentence that carries the stale token already says the step is built. It still gets the token fix, because a reader scanning openings reads the token.

### WHAT THE FIX IS, AND WHAT IT IS NOT

"THE OPENING" MEANS THE WHOLE OPENING LINE, AND THAT IS THE UNIT EVERYWHERE IN THIS STEP. These sidecars are not hard-wrapped, so the opening line IS the opening paragraph, and every opening the selector reaches is exactly one line. Criterion 1 already reads the first non-blank, non-heading LINE, and criterion 5 bounds the diff per LINE, so the line is the unit the method and the criteria already use, and it is the unit the obligation below uses too. An earlier draft of this paragraph scoped the obligation to the opening SENTENCE while directing an edit inside `instrument-flag`'s SECOND sentence, which is the inconsistency this paragraph exists to remove.

THE FIX IS A DELETION, NOT A SUBSTITUTION (`Q-78-statusform`). The leading status token GOES and no corrected label replaces it, because the protocol sentence quoted at the top says a Step Detail does not repeat the status label, and the Roadmap table `render` generates from `[[step]].status` already carries it for every step.

THE OBLIGATION IS SEMANTIC, NOT LEXICAL. After the fix, the opening line of each selected file must contain no claim that the step's declared `status` contradicts, and must read as correct prose. Deleting the leading status token is necessary and is nowhere near sufficient on either count, which is DEMONSTRATED rather than argued below.

A PLAIN STRIP IS NOT THE FIX, MEASURED. Applying a bare token deletion to the enumerated contradicting openings in a throwaway copy leaves seven that do not begin as prose: `state-schema` becomes "and non-blocking (`Q-11`); ...", `workflow-calibration` becomes "(deferred). The scaffolded workflow's ...", `instrument-flag` becomes "optional. The design is decided below ...", `reviewer-reproducible-evidence` becomes "(built first, before resuming the paused ...)", and `optional-modules`, `structured-skeleton` and `workflow-driver` each begin mid-sentence in lower case. Criterion 12 is the oracle for this and it flags exactly those seven on that fixture. Each needs an AUTHORED opening, not a strip. The rest strip cleanly, most of them to "Decided (...", "Scheduled by the human ..." or "This is a/an ...", and `decision-receipt` to "The first `Q-44` structured-data pilot ...". THE WIDENED FILES WERE NOT PUT THROUGH THAT FIXTURE, and nothing here says they all strip cleanly. Criterion 12 runs over the whole selected set and is what finds the ones that do not.

THE DEMONSTRATION, run on a throwaway copy of `docs/` and reproducible under the EARLIER selector, kept because it is what proved the token-only fix insufficient. Take three of the enumerated openings and replace ONLY the leading token, leaving the rest of the line: `workflow-driver` "Not started;" to "In progress;", `optional-modules` "Not started;" to "Complete;", `workflow-calibration` "Not started (deferred)." to "In progress.". The earlier selector then certified all three as clean while they read:

- "In progress; the build-start is a separate pending human decision, so this umbrella step stays `not-started` with no increments declared yet".
- "Complete; a design-questions pass is needed before implementing (as with `git-url-fetch`)".
- "In progress. ... This is deliberately deferred: ... so nothing here blocks earlier work".

The widened selector rejects all three outright, because each still carries a token. That is a second reason the widening is the stronger scope, and it does not remove the need for criterion 11's reading, because a file with no token can still carry a false claim in the rest of its opening line.

WORKED EXAMPLES, MEASURED, AND THIS LIST IS A FLOOR RATHER THAN A SET. Each is a claim in the opening line that the declared `status` contradicts. NOTHING HERE CLAIMS THEY ARE THE ONLY ONES, and an earlier draft of this paragraph did claim exactly that, named two, and was wrong; a later count of five was also short. The implementer reads each selected opening line whole and disposes of what it finds, using criterion 10 as the starting list.

- `workflow-driver`: "so this umbrella step stays `not-started` with no increments declared yet". False twice over on an `in-progress` step that declares three increments.
- `workflow-calibration`: "This is deliberately deferred: ... so nothing here blocks earlier work". Same class as the front-matter contradiction excluded below, inside the very line being edited.
- `optional-modules`: "a design-questions pass is needed before implementing". False on a `complete` step.
- `instrument-flag`: "The design is decided below; the build is deferred". The build is not deferred on a `complete` step.
- `decision-folder-currency`: "FOUR passages are still out of step with the rule, three of them in `pack/prompts/orchestrator.md` and one in `pack/AGENTS.md`". Measured at HEAD, all four now name the planner: `pack/prompts/orchestrator.md:27` ("which is the planner's job and which you route to it rather than author yourself"), `:31` ("you route to the planner to author rather than editing the plan yourself"), `:33` ("and the planner authors that fold when it is non-trivial") and `pack/AGENTS.md:63` (the same clause). `decision-folder-currency` is the step that fixed them, and it is `complete`, so "are still out of step" is false of the tree its own sidecar sits in.
- `planner-folds-decisions`, WHICH IS ALSO CRITERION 11'S WITNESS. Its opening asserts that AGENTS.md names the planner as decision-folder only on the request-interrupt path, that the human-input-contract paragraph uses actor-less passive voice, and that the Socratic-mode paragraph only says the mode reuses the intake and Open-Questions machinery. All false at HEAD: `AGENTS.md:41` reads "the orchestrator routes it to the planner to author, as on the request-interrupt path above, rather than editing the plan directly", `AGENTS.md:43` reads "its non-trivial fold routed to the planner to author as above rather than edited in directly", and `pack/AGENTS.md` carries both. The step's own title is "name the PLANNER as the folder of every non-trivial decided decision at the human-input-contract / Socratic-mode / checkpoint points", so its `complete` status falsifies its own opening.

ONE THING THAT IS DRIFT BUT IS NOT A CONTRADICTION, so it is not treated as though it were. `instrument-flag` opens "Not started; optional.", and deleting only the leading token leaves a bare "optional." fragment. That fragment is not a self-contradiction, because `optional` says nothing about whether work started. It is still in scope under the widened rule, because `optional` is a `status` value the TOML does not carry for that step, and it is a second label to delete rather than a contradiction to resolve.

THREE BORDERLINE CASES, RULED HERE SO THE IMPLEMENTER DOES NOT HAVE TO GUESS, now that the fix is a deletion.

- `state-schema` opens with the compound "Deferred and non-blocking (`Q-11`);", so the token cannot be lifted out on its own. Author an opening that keeps the non-blocking fact and the sequencing if they are still true, and states no status at all.
- `reviewer-reproducible-evidence` opens "Next (built first, before resuming the paused `code-value-audit-static` step 86)." Deleting "Next" strands the parenthetical, so this one is authored too. The build-order fact stays and `code-value-audit-static` is still `in-progress`, so the parenthetical's content is true. Only its leading token and its orphaned shape are the problem. NOTE THAT "step 86" IS A CITATION BY `order` VALUE, which `plan-order-array-position` increment 2 restates by slug. Whichever step runs second leaves the other's edit intact, because the two touch different parts of the same line.
- `checks-runner-worktree-name-collision` strips CLEANLY, measured: deleting "Deferred. " leaves "This is a defect fix in `src/`, not a design change: ...". Its borderline half is its closing "The human's call (2026-07-28) was to TRACK it here rather than schedule it now", which is a dated record of a past decision and stays as history.

NOT IN SCOPE, EACH CONSIDERED AND EXCLUDED SO THE ABSENCE IS A CHOICE.

- WHAT ANY OF THESE STEPS SHOULD SAY. This step corrects a stale claim about state. It does not review, re-scope, re-title or rewrite a single step, and it changes no step's `status`, which is the orchestrator's.
- THE FRONT MATTER. `docs/plans/agent-scaffold._status-narrative.md` lists `workflow-calibration` under "Still optional/deferred", which is the same contradiction in the plan's most-read paragraph. It is left alone deliberately: that file is a long historical accretion whose currency is a separate and much larger question, and correcting one clause inside it invites the rewrite this step exists to avoid. It is recorded here so it is a named exclusion rather than a miss.
- ANY SIDECAR TEXT BELOW THE OPENING LINE. No sweep was run for stale claims below the opening line, in any sidecar, so nothing here says there are none. In particular, `workflow-calibration.md`'s stale audit-record inventory is not edited here: that file is on the handover list, criterion 4 forbids this step from opening it, and routing a non-opening correction through this increment made criteria 4 and 6 unsatisfiable together. A later currency pass or the successor step's planner may scope that sentence explicitly; this step makes no claim to close it.
- A STEP OPENING THAT SITS BELOW AN UMBRELLA PARAGRAPH, WHICH IS THE ONE CLASS THIS SELECTOR CANNOT REACH AND THE REASON THE CLOSURE CLAIM ABOVE IS QUALIFIED. Criterion 1 reads the first non-blank non-heading line of the FILE. Five sidecars open with an umbrella paragraph that carries context shared by several steps, so in those five the selector reads the umbrella paragraph and never reaches the step's own opening. `docs/plans/agent-scaffold.steps/exploration-mode.md:7` opens "Next." on a `complete` step and is invisible to criterion 1 for exactly that reason. It is excluded because admitting it would replace a reproducible selector with a reading, which is the trade this step's first line refuses. `Q-79` records the gap and closes it as a side effect, because the typed-umbrella work moves the umbrella paragraph out of those five files and the selector then reaches their step openings. Re-run criterion 1 after that work lands.
- `pack-rebuild-tracking`, WHICH IS A REAL INSTANCE OF THE UNDERLYING PROBLEM AND IS STILL EXCLUDED. Its opening reads "To be done before the golden sync-test in `ledger-template`" on a `complete` step whose named successor `ledger-template` is also `complete`. It is excluded because this step's first line fixes the FILE SET by a command, and "To be done" is not a Roadmap status label, so criterion 1 does not select it. Criterion 10's word list does contain `to be done`, so its regex would match this opening; what keeps it out is criterion 10's worklist, not the absence of a rule that reaches it. Admitting a file that only a reading selects would replace a reproducible selector with a judgement, and would break criterion 1 (which would stay silent on it) and criterion 4 (which pins the changed set to the captured worklist).
- THE PACK PROSE RULE. `pack/plan-template.documentation-protocol.md` carries no sentence about a sidecar restating a `[[step]]` field, so no scaffolded project inherits the rule this plan states at `docs/plans/agent-scaffold.documentation-protocol.md:5`. `step-intent-encoding` increment 3 ships that sentence, because that increment already edits the pack and so already owes the rendered-pair check. This step touches no file under `pack/`.
- THE HANDOVER LIST, WHICH IS THE 19 OPENINGS THAT NEED AN AUTHORED REPLACEMENT. The human split them out on 2026-08-21 (`q_id:"Q-78-driftsplit"`). THIS SIDECAR IS THE POINTER AND NOT THE STEP: it declares the boundary, records the commands that produce the list and the figure the list holds today, and stops there. The successor step, with its own increments, its own risk classes and their `AGENTS.md:56` grounds, and its own acceptance criteria over an authored opening, is a planner's to author against this sidecar as its input. Nothing is written here for it to inherit, because a stub authored inside a fix pass is the crossing this loop already recorded once.

### ACCEPTANCE, each executable

1. THE SELECTOR PRINTS EXACTLY THE HANDOVER LIST, AND NOTHING ELSE. Run this from the repository root. It drives off the generated Roadmap table so it needs no JSON tool, and under the widened scope it reports ANY status token rather than a mismatched one, so no exemption rule and no status comparison appear in it.

```
sed -n 's/^| `\([a-z0-9-]*\)` | \([a-z ]*\) |.*/\1/p' docs/plans/agent-scaffold.md |
while read -r slug; do
  line=$(sed -e '/^#/d' -e '/^[[:space:]]*$/d' "docs/plans/agent-scaffold.steps/$slug.md" | head -1)
  token=$(printf '%s' "$line" |
    grep -oE '^(Not started|In progress|Complete|Skipped|Next|Optional|Deferred)([.;,:]|$| \(| (and|by|but|or|nor|for|so|yet|until|unless|pending|while|after|before|because|since|though|although|then|with|without|on|in|at|to|from|as|per)\b)' |
    grep -oE '^(Not started|In progress|Complete|Skipped|Next|Optional|Deferred)')
  [ -n "$token" ] && printf '%s\t%s\n' "$slug" "$token"
done
```

THE TOKEN IS ANCHORED ON A SENTENCE BOUNDARY, AND THAT ANCHOR IS WHAT FIXES THE SCOPE. A status word is a LABEL when the token is followed by punctuation, by an opening parenthesis, by end of line, or by a conjunction or preposition. It is an ORDINARY ADJECTIVE when a noun follows it, and an adjective is not a second copy of anything the TOML owns. AN EARLIER FORM ALLOWED A BARE SPACE AFTER THE TOKEN, which admitted the nine `Deferred <noun>` openings enumerated above and would have directed nine correct sentences to be rewritten. Run this alongside it, and record its output in the outcome:

```
sed -n 's/^| `\([a-z0-9-]*\)` | \([a-z ]*\) |.*/\1/p' docs/plans/agent-scaffold.md |
while read -r slug; do
  line=$(sed -e '/^#/d' -e '/^[[:space:]]*$/d' "docs/plans/agent-scaffold.steps/$slug.md" | head -1)
  printf '%s' "$line" | grep -qE '^(Not started|In progress|Complete|Skipped|Next|Optional|Deferred)([.;,: ]|$)' || continue
  printf '%s' "$line" | grep -qE '^(Not started|In progress|Complete|Skipped|Next|Optional|Deferred)([.;,:]|$| \(| (and|by|but|or|nor|for|so|yet|until|unless|pending|while|after|before|because|since|though|although|then|with|without|on|in|at|to|from|as|per)\b)' && continue
  printf '%s\t%s\n' "$slug" "$(printf '%s' "$line" | cut -c1-46)"
done
```

THE RELATION, NOT THE THREE NUMBERS: the relaxed form reaches a superset of the anchored form, and this complement prints exactly the difference between them. The paragraph at THE MEASUREMENT THE SPLIT DIRECTED, above the increment block, states what the three commands printed on the day and carries the commands that reproduce them. No figure is written into this criterion, because a figure here is a pass condition that the next sidecar edit falsifies. MEASURED, every row this complement prints today is an adjectival opening on a step declared `deferred`. THE COMPLEMENT IS PRINTED RATHER THAN ASSUMED EMPTY, because the connective list is a closed list, and a tenth opening using a connective the list does not carry would otherwise drop out of scope silently. Every row it prints is disposed of in the outcome, as an adjectival opening left alone or as a connective the list must gain.

Pass: the SLUG COLUMN of the anchored selector's output is identical to the SLUG COLUMN of the HANDOVER list criterion 2 captured. Compare the first columns and nothing else, under bash:

```
comm -3 <(cut -f1 anchored.txt | sort) <(cut -f1 handover.txt | sort)
```

Pass: that command prints nothing. THE COMPARISON IS ON THE FIRST COLUMN BECAUSE THE TWO SECOND COLUMNS CANNOT MATCH, WHICH IS MEASURED. This criterion prints `slug<TAB>TOKEN`, where the token is the bare status word. Criterion 12 prints `slug<TAB>` and the first 46 characters of the stripped line. On the same slug the two second columns read `Deferred` and `and non-blocking (`Q-11`); sequenced after the`. AN EARLIER FORM REQUIRED THE TWO OUTPUTS TO BE IDENTICAL, and MEASURED on a throwaway copy given a correct fix, `cmp` on the whole rows reports them different on every row while `comm -3` on the first columns prints nothing. So the earlier form refused a correct implementation and bought a wrong one nothing.

It is the oracle for the LEADING TOKEN and the pipeline's exit status is not, and it is a bash snippet, so run it through the project's bash prefix rather than in nu. MEASURED before the fix it prints one row for every sidecar in scope, including every slug the enumeration above names, so it detects the condition rather than merely staying silent. MEASURED after a correct fix it prints exactly the handover rows. NO ROW COUNT IS WRITTEN HERE, because the selected set moves as sidecars are edited, and criterion 2 records what the commands print on the day. A DELETION SATISFIES IT BY CONSTRUCTION, since a line with no leading status token cannot print one, which is why this criterion cannot be the evidence that the deletion was done well; criteria 10, 11 and 12 carry that.

CRITERION 1 IS NECESSARY AND NOT SUFFICIENT, WHICH IS MEASURED. On the three-file demonstration above, the earlier selector printed nothing for those three while all three lines contradicted themselves; the widened selector rejects them, but only because they still carry a token. A file whose token is deleted and whose remaining line is still false passes this criterion. Criterion 10 is what covers the rest of the line. Do not read criterion 1 as the step being done.

2. THE WORKLIST IS CAPTURED BEFORE THE EDIT, AND THE SPLIT IS DECIDED BY A COMMAND RATHER THAN BY A READING. THE THREE CAPTURE FILES ARE NAMED HERE, because criterion 1's pass condition reads two of them by name. Write them to a scratch directory OUTSIDE the repository.

- `anchored.txt` is criterion 1's anchored selector run against the pre-increment tree. That is the SELECTED SET, and the outcome records it VERBATIM.
- `handover.txt` is what the command below prints. The slugs it prints are the HANDOVER LIST, which this step does not touch and the successor step owns.
- `worklist.txt` is `comm -23 <(cut -f1 anchored.txt | sort) <(cut -f1 handover.txt | sort)`. That is THIS STEP'S WORKLIST, and it is the worklist for criteria 4, 5, 6, 10, 11 and 12.
- `opening-anchors.tsv` captures, for every worklist slug, the first 48 characters of the opening after the mechanical token-and-delimiter deletion, before any edit. Criterion 6 requires that exact anchor to remain at the start of the post-edit opening, so a replacement or a whole-line deletion cannot masquerade as the decided deletion.

THE HANDOVER CAPTURE IS WRITTEN OUT AS A COMMAND OF ITS OWN AND IS NOT A SUBSTITUTION INTO CRITERION 12, AND THE REASON IS CIRCULARITY RATHER THAN STYLE. Criterion 12's command hard-codes the LIVE path root `docs/plans/agent-scaffold.steps/$slug.md`, so pointing it at a throwaway copy needs a substitution this sidecar would otherwise never write out, and its `$WORKLIST` is THIS STEP'S WORKLIST, which the bullet above defines as `anchored` minus `handover`, so its input set is defined in terms of the very file this capture produces. MEASURED, criterion 12's command run VERBATIM against the live tree prints ZERO rows, because every selected opening still begins with an uppercase status token, and an implementer who copies the command rather than reading the prose gets an EMPTY `handover.txt`. From there the worklist becomes the whole selected set and the criteria jointly endorse doing the successor step's work inside this step, which falsifies this increment's premise half and the human decisions `Q-78-driftsplit` and `Q-78-drifthandover`. The command below breaks the circle by naming its own path root and taking the SELECTED SET as an explicit argument. Save it in the scratch directory, then run it under bash FROM THE REPOSITORY ROOT with BOTH arguments absolute: `bash <scratch>/h1.sh <scratch>/anchored.txt <scratch>/steps-copy > <scratch>/handover.txt`. The copy path must not yet exist.

```
#!/usr/bin/env bash
# H1: the handover list. Strip the leading token from every SELECTED file's opening line
# in a throwaway copy, then apply criterion 12's test to that copy.
SELECTED="$1"; COPY="$2"
cp -r docs/plans/agent-scaffold.steps "$COPY"
while IFS=$'\t' read -r slug token; do
  n=$(grep -nvE '^#|^[[:space:]]*$' "$COPY/$slug.md" | head -1 | cut -d: -f1)
  sed -i "${n}s/^\(Not started\|In progress\|Complete\|Skipped\|Next\|Optional\|Deferred\)[.;,:]\{0,1\}[[:space:]]*//" "$COPY/$slug.md"
done < "$SELECTED"
while IFS=$'\t' read -r slug token; do
  line=$(sed -e '/^#/d' -e '/^[[:space:]]*$/d' "$COPY/$slug.md" | head -1)
  case "$line" in
    [A-Z]*|'`'*) ;;
    *) printf '%s\t%s\n' "$slug" "$(printf '%s' "$line" | cut -c1-46)" ;;
  esac
done < "$SELECTED"
```

The selected-set path is never read relative to the repository root or to the copy. MEASURED, the earlier script read bare `anchored.txt`: from the repository root it printed an empty handover after `cut` failed, and from the scratch directory its `cp docs/...` failed; both modes exited 0 and made the later criteria endorse the wrong split.

After `worklist.txt` is captured, build the provenance anchors from the PRE-increment tree:

```
while read -r slug; do
  line=$(sed -e '/^#/d' -e '/^[[:space:]]*$/d' "docs/plans/agent-scaffold.steps/$slug.md" | head -1)
  stripped=$(printf '%s' "$line" | sed 's/^\(Not started\|In progress\|Complete\|Skipped\|Next\|Optional\|Deferred\)[.;,:]\{0,1\}[[:space:]]*//')
  printf '%s\t%s\n' "$slug" "$(printf '%s' "$stripped" | cut -c1-48)"
done < <scratch>/worklist.txt > <scratch>/opening-anchors.tsv
```

Replace each `<scratch>` with the same absolute scratch-directory path. The outcome records the anchor file verbatim; no row count is a pass condition.

THE STRIP REMOVES THE TOKEN AND ITS DELIMITER, NOT THE TOKEN ALONE, which is what makes the capture agree with the measurement under WHAT THE FIX IS: deleting "Deferred. " from `checks-runner-worktree-name-collision` leaves "This is a defect fix in `src/`, ...", while deleting "Deferred" alone would leave a leading full stop and flag a file that strips cleanly. The second loop is criterion 12's body verbatim, with the copy as its path root and the selected set as its input, so the two criteria apply one test and not two. NO ROW COUNT IS WRITTEN HERE, for the reason the paragraph below records.

Criterion 1's pass condition is then re-run against `anchored.txt` taken from the POST-increment tree, against the same `handover.txt` this criterion captured.

NO FIGURE IS WRITTEN INTO THIS CRITERION. The three counts move as sidecars are edited, so the outcome records what the commands print on the day, and the paragraph at THE MEASUREMENT THE SPLIT DIRECTED, above the increment block, is the one dated statement of them. THE RELATION THAT HOLDS: the selected set is the disjoint union of the handover list and this step's worklist. The captures are written to a scratch directory outside the repository, or to files the increment deletes before it commits, so criterion 4's changed-path set stays clean. THE LISTS ARE CAPTURED RATHER THAN WRITTEN INTO THIS SIDECAR, because a slug list written here is a snapshot that expires the next time a sidecar changes, and this step exists to remove snapshots of exactly that kind.

3. RED THEN GREEN, demonstrated rather than asserted. Revert one corrected opening to its stale token, show criterion 1 prints that one row in addition to the handover list, restore it, show criterion 1 back at the handover list. The red output lands as evidence in the outcome.

4. THE CHANGED PATH SET IS THIS STEP'S WORKLIST PLUS THE PROJECTION. `git diff --name-only` over the step's change lists exactly the sidecars on THIS STEP'S WORKLIST from criterion 2, under `docs/plans/agent-scaffold.steps/`, plus the regenerated `docs/plans/agent-scaffold.md`. NO FILE ON THE HANDOVER LIST APPEARS, and its absence is the point: the successor step owns those files and edits each of them once. No file under `src/`, `pack/` or `tests/` appears, `docs/plans/agent-scaffold.plan.toml` does not appear (no `status` is flipped, and the step's own entry is authored by a planning pass rather than by the implementation), and `docs/metrics/workflow.jsonl` does not appear. No count of paths is stated, because the worklist is the count.

5. THE CHANGE IS CONFINED TO THE OPENING LINE AND ADDS NO PARAGRAPH. For each file on the worklist, `git diff --numstat` reports at most 2 added and 2 removed lines, since each opening is one line in an unwrapped file and the fix rewrites that line in place rather than splitting it.

AN ACCEPTED RESIDUAL SITS IN THAT BOUND, RECORDED HERE RATHER THAN REPAIRED. The bound is one line-pair per file LOOSER than its own justification: a one-line in-place rewrite is one added and one removed, not two and two. The human accepted it on 2026-08-22 as residual risk, receipt `type:"decision"` `q_id:"Q-78-round4"` in `docs/metrics/workflow.jsonl`. WHAT A WRONG IMPLEMENTATION COULD THEN SHIP: one silent, unrelated paragraph rewrite in each file on the worklist, in text `render` publishes into every reader's copy of the plan, that no criterion of this step reads. Criteria 1, 6, 10, 11 and 12 read the opening line, criterion 4 checks path names, and criterion 7 re-renders whatever is there. WHAT BOUNDS IT: criterion 6 forbids additions in the opening itself, the whole-diff read a reviewer does at merge, and the NOT IN SCOPE bullet that excludes ANY SIDECAR TEXT BELOW THE OPENING LINE. THE COST ACCEPTED is that this numstat bound alone still admits an edit outside the opening line and an added line not balanced by a removed one. The repair, one character in each of the two numbers, stays available.

6. THE PRE-EDIT OPENING PROVENANCE SURVIVES, AND THE CHANGE IS DELETION-ONLY. Record the increment's named base commit in the outcome and run this under bash from the repository root, passing the absolute `opening-anchors.tsv` path from criterion 2:

```
#!/usr/bin/env bash
ANCHORS="$1"; BASE="$2"
checked=0; bad_anchor=0; added=0
while IFS=$'\t' read -r slug anchor; do
  checked=$((checked+1))
  path="docs/plans/agent-scaffold.steps/$slug.md"
  line=$(sed -e '/^#/d' -e '/^[[:space:]]*$/d' "$path" | head -1)
  case "$line" in
    "$anchor"*) ;;
    *) echo "OPENING LOST PROVENANCE $slug"; bad_anchor=$((bad_anchor+1));;
  esac
  additions=$(git diff --word-diff=porcelain -U0 "$BASE" -- "$path" | grep -E '^\+' | grep -v '^\+\+\+' || true)
  if [ -n "$additions" ]; then
    echo "OPENING ADDS WORDS $slug"
    printf '%s\n' "$additions"
    added=$((added+1))
  fi
done < "$ANCHORS"
printf 'checked=%d bad_anchor=%d added=%d\n' "$checked" "$bad_anchor" "$added"
```

Pass: `checked` equals the worklist row count, `bad_anchor=0` and `added=0`. The anchor is captured AFTER mechanically removing the token, so a correct token deletion preserves it; later false clauses may be removed, but no replacement sentence is authored. A generic sentence replacing every opening fails `bad_anchor` on every row. Deleting a whole opening line where the next paragraph happens to begin as prose also fails its anchor. This is the premise-half oracle that criteria 1, 4, 5 and 12 do not supply.

7. THE PROJECTION IS REGENERATED AND MATCHES. `./target/debug/agent-flow render docs/plans/agent-scaffold.plan.toml` then `./target/debug/agent-flow render --check --strict docs/plans/agent-scaffold.plan.toml` exits 0, so the committed `docs/plans/agent-scaffold.md` carries the corrected Step Details. `docs/plans/agent-scaffold.md` is never hand-edited.

8. THE VALIDATORS AND THE SUITE STAY GREEN. `./target/debug/agent-flow validate --source docs/plans/agent-scaffold.plan.toml --metrics docs/metrics/workflow.jsonl` and `./target/debug/agent-flow validate --source docs/plans/agent-scaffold.plan.toml --workflow` both exit 0, and `cargo test` passes. None of the three reads sidecar prose, so they are the no-regression check rather than the oracle for this step. Criterion 1 is the oracle.

AN ACCEPTED RESIDUAL SITS IN THE FIRST COMMAND, RECORDED HERE RATHER THAN REPAIRED. That command is judged on its exit code alone, which is the exact pattern `validate-missing-source-exit` exists to remove, and that step's own "this step blocks nothing" paragraph names three of the `Q-78` pass's five steps and not this one. The human accepted it on 2026-08-21 as residual risk, receipt `type:"decision"` `q_id:"Q-78-residuals"` in `docs/metrics/workflow.jsonl`. THE CONSEQUENCE THE HUMAN WEIGHED: nothing wrong can ship, because this criterion requires the SECOND command to exit 0 as well, and MEASURED in an empty directory outside the repository `validate --source <absent> --workflow` exits 1 with `--workflow requested but no plan source resolved`. So an absent source fails this criterion on the `--workflow` arm. The cost accepted is that the `Q-78` pass ships one sidecar carrying the pattern one of its own steps removes.

9. ASCII ONLY. `LC_ALL=C grep -cP '[^\t\x20-\x7e]' <file>` prints 0 for every changed file. Use that pattern rather than `[^ -~]`, which matches every hard tab. Note `grep -c` exits 1 when the count is 0, so it breaks an `&&` chain.

10. EVERY ROW OF THE DISPOSITION LIST IS DISPOSED OF. This is NOT a pass-or-fail oracle and its empty output is not the target; a whole-line keyword scan cannot be one, because several matches are legitimate prose (`reviewer-harness-field` describes an "OPTIONAL `harness` string", `workflow-invariants` records that the `ledger-parse` keystone was SKIPPED). It is a bounded worklist over THIS STEP'S WORKLIST from criterion 2. Paste those slugs into `WORKLIST` and run it under bash; it uses no process substitution. Unlike the earlier form, it does NOT excuse a hit that equals the declared `status`, because under the widened scope a matching token is in scope too.

```
WORKLIST="<THIS STEP'S WORKLIST from criterion 2, space-separated>"
for slug in $WORKLIST; do
  line=$(sed -e '/^#/d' -e '/^[[:space:]]*$/d' "docs/plans/agent-scaffold.steps/$slug.md" | head -1)
  hits=$(printf '%s' "$line" |
    grep -o -i -E 'not started|not-started|in progress|in-progress|complete|skipped|next|optional|deferred|to be done|still|pending|not yet|before implementing' |
    tr 'A-Z' 'a-z' | sed 's/-/ /' | sort -u | tr '\n' ' ')
  [ -n "$hits" ] && printf '%s\t%s\n' "$slug" "$hits"
done
```

MEASURED before the fix, it prints one row per worklist file, because each still carries its own leading token. THE OBLIGATION: the outcome records, for every row this prints after the fix, either the correction made or the reason that match is legitimate prose. A row left with neither is the step not finished. The scan is a FLOOR and not a census: `planner-folds-decisions`' false clauses carry no status-adjacent vocabulary, because they are present-tense assertions about the wording of another file, so once its leading token is corrected this command stops flagging it while every one of those clauses is still false. That is why criterion 11 exists.

11. EVERY OPENING LINE WAS READ WHOLE. The outcome states, for each file on THIS STEP'S WORKLIST, that its opening line was read end to end against the step's declared `status`, and records what that reading found beyond what criterion 10 flagged. This is the criterion that is a judgement rather than a command, and it is here because the measured evidence says no command covers the class: the demonstration under WHAT THE FIX IS shows a token-only fix leaving false prose, and `planner-folds-decisions` goes silent under criterion 10 once its leading token is corrected while every assertion the bullet above records is still false. That silence survives an enlarged word list. MEASURED OVER THIS STEP'S WORKLIST, adding `remains`, `awaiting`, `planned`, `to do`, `not done`, `when built`, `blocked`, `paused`, `will be` and `yet to` picks up `doc-redundancy-cleanup` on `when built`, and does not reach `planner-folds-decisions`, which is on the worklist. THE SCOPE IS NAMED BECAUSE AN EARLIER FORM STATED A PAIR THAT REPRODUCED ON NEITHER SCOPE: its second slug was `reviewer-reproducible-evidence` on `paused`, and that file is on the HANDOVER list, which criterion 11 never reads. What carries this criterion is the last clause and not the row it names: an enlarged list leaves `planner-folds-decisions` silent while every assertion the bullet above records about it is still false, and the outcome records what the enlarged list prints on the day.

12. NO OPENING IS LEFT UNGRAMMATICAL BY THE DELETION, AND NO OPENING STATES A STATUS. Run this from the repository root under bash with the same `WORKLIST` as criterion 10; it prints nothing when every opening line on the worklist begins as prose. MEASURED on a throwaway copy of the enumerated contradicting openings given a bare token deletion, it printed exactly the seven named under WHAT THE FIX IS, so it detects the condition rather than merely staying silent. The widened files were not put through that fixture, so this criterion is also how they are checked for the first time.

```
for slug in $WORKLIST; do
  line=$(sed -e '/^#/d' -e '/^[[:space:]]*$/d' "docs/plans/agent-scaffold.steps/$slug.md" | head -1)
  case "$line" in
    [A-Z]*|'`'*) ;;
    *) printf '%s\t%s\n' "$slug" "$(printf '%s' "$line" | cut -c1-46)" ;;
  esac
done
```

MEASURED ON THIS STEP'S WORKLIST, a bare token deletion leaves every file beginning as prose, so a correct implementation prints nothing. THE FILES THAT DO NOT ARE PRECISELY THE HANDOVER LIST criterion 2 captured, and this command over the whole selected set is what produced that list. No count is written here, for the reason criterion 2 records. Run it here over THIS STEP'S WORKLIST only, because the handover files keep their token and would fail a criterion whose repair is not this step's.

CRITERION 12 IS NECESSARY AND NOT SUFFICIENT FOR THE FIRST HALF OF ITS OWN HEADER, WHICH IS MEASURED. The command tests one thing, whether the opening's first character is an uppercase letter or a backtick. On the bare-strip fixture, capitalising the first character of `state-schema`'s opening and changing nothing else takes it out of this criterion's output while it reads "And non-blocking (`Q-11`); sequenced after the human-interface work", which is a fragment. Criterion 1 stays silent on it, and criterion 10 flags it only on "before implementing", which is legitimate prose in that sentence, so an implementer meeting criterion 10's obligation records it as legitimate and moves on. Do not read an empty criterion 12 as certifying the openings. The semantic bar is the one under THE OBLIGATION IS SEMANTIC above, and criterion 11's reading is what carries it. The second half of this header is a reading, not a command: no opening line on the worklist names a Roadmap status for its own step at all, which is what `docs/plans/agent-scaffold.md:26` requires and what criterion 1's empty output cannot show.

### DOCUMENTATION IMPACT

None outside `docs/plans/`. `grep -rn 'sidecar' pack/ README.md` returns matches, and they were read rather than counted: they describe the skeleton-plus-sidecar structure, the render path, and the `<task>.steps/<slug>.md` naming convention. The one that comes closest to prescribing a sidecar's content is the scaffolded template `pack/plan-template.steps/example-step.md` and its rendered copy `docs/plans/TEMPLATE.steps/example-step.md`, one heading plus "What this step does and how; once done, the outcome and the evidence." It asks for what the step does and how, not for a state, so the deletion agrees with it and no shipped guidance, prompt or template goes stale. The pack is untouched by this step.

`docs/plans/agent-scaffold.documentation-protocol.md` is NOT edited either. This step brings the selected files into line with the sentence that file already carries; it does not change the sentence. It DOES now make the sentence true of every sidecar the selector reaches, which the earlier narrower scope did not, and the one class the selector cannot reach is the umbrella-owner exclusion named above.

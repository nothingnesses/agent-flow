The corrected decision-ready synthesis is `docs/plans/workflow-calibration.explorations/Q-86-synthesis.md`. Q-86 remains `open` for two human choices through the human-input contract.

- Choose `A - Sealed phase budget`, `B - Frozen obligations with sealed phase campaigns`, or `C - Fixed-depth verification protocol`.
- Choose the serious terminal floor `high` or `critical`, or explicitly defer that floor to a separate decision before implementation.

The synthesis recommends B at medium to low confidence because its corrected complete-finding-map proof combines a non-resettable authority envelope with a finite structured completion predicate. Each post-freeze B phase has a clean minimum of `n_q + 1` review batches and a maximum of `4n_q + 1`. B plan review separately reuses A's sealed controller. Its inherited and still-unapproved values are five normal batches, two reserve batches, at most six grouped repairs, reserve unlock after a triage-valid high or critical or an overturned serious dismissal, and clean completion of one batch at low risk or two consecutive batches at risky. Thus `r_plan` is one or two. B's whole-family minimum is `r_plan + |O| + m + 1`, and its maximum is `4|O| + m + 8`. B therefore has ordinary review cost that scales with the future frozen obligation count, unlike A and C. Its post-freeze phase factor is enumerated, while the family bound follows by exact-owner summation with no transfer plus the separate plan controller.

The Q-78 log observes four named folds but carries no prospective B obligation digests. Under the explicit assumption that those folds each change the structured obligation or exclusion digest, B would require four terminal replans, four additional human receipts, and four additional plan-review and freeze campaigns. Those costs are conditional, not reconstructed historical facts. A would stop at pass seven with three later low shortfalls undiscovered. C would stop at pass three with 23 later shortfalls undiscovered, including further highs. At floor `high`, C enters `SeriousBlocked`. At floor `critical`, residual acceptance of the open high is available before those 23 shortfalls are discovered.

The corrected proof exhausts atomic batches whose complete stable finding maps have cardinality two across all four severities. It includes same-owner and cross-owner low plus critical, mixed owned and unowned batches, and parent plus fix-induced child. Every affected obligation advances in the same transition, and the owner-product invariant preserves matching obligation and finding states in both directions. A batch can satisfy several finding-bearing initial attempts, but an absent non-primary owner receives no clean credit, so B's clean minimum remains `n_q + 1`. The proof also controls A's remaining-clean-suffix foreclosure and upheld-dismissal reserve path, C's mandatory blind closure on every ordinarily completing acceptance path at both risks, B's `LegacyNoRubric`, initial-attempt priority, `UnownedInScopeFinding`, untested residual delivery, and successor receipts bound to both family ids, both scope digests, presented options, chosen successor, predecessor spend, and the complete carried finding map.

The selected shared scope-expansion policy backlogs low, sends medium to the terminal human scope decision, and refers proposed high or critical out-of-scope rulings to independent re-check. An upheld high goes to the terminal human scope decision and an overturned high returns in scope. Either critical result is `SeriousBlocked`. The recommended floor is `high`. It removes ordinary residual acceptance and ordinary narrowing while a high or critical is open. The mandatory minimum is `critical`, which preserves more human discretion but permits residual acceptance of a high. Neither floor is selected yet.

The current mechanism remains non-admissible comparison evidence. The severity-decay candidate remains excluded until prospective trajectory evidence is trustworthy. A failed B schema and reconstruction proof of concept must return to the human with A as the recommendation rather than selecting A automatically. Q-85 approved only the design pass. No mechanism, floor, controller constant including the five-normal-plus-two-reserve slice used by A and inherited by B plan review, B's one reopen, or C's four-stage and two-repair depth, pack change, or implementation is approved until the human decisions are receipted and folded into `workflow-calibration`.

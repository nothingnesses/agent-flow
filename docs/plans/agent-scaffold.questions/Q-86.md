The narrowed decision artefact is `docs/plans/workflow-calibration.explorations/Q-86-synthesis.md`. Q-86 is `decided -> folded into bounded-convergence-option-b-proof`.

Q-88 ended the five-round capped proof-before-choice artefact. The narrowed artefact then reached its own five-round cap with two low findings in round 5. Q-89 retired the capped narrowed counters and authorised focused mixed-model verification of the two low fixes. That verification found one low current-state wording defect in the structured Q-86 ask. Q-90 authorised the exact correction and merge without another review. The narrowed decision artefact is closed. The retained `q86-controller-proof.py` and `q86-q78-scope-replay.sh` scripts remain adversarial prototypes rather than recommendation-eligibility proofs. The current workflow and code remain unchanged.

## Decision

The exact architecture options, in order, were:

1. `A - Sealed phase budget`.
2. `B - Frozen obligations with sealed phase campaigns`.
3. `C - Fixed-depth verification protocol`.

The recommendation was `B - Frozen obligations with sealed phase campaigns`. The human chose `B - Frozen obligations with sealed phase campaigns`.

The exact `type:"decision"` receipt in `docs/metrics/workflow.jsonl` records those three options, that recommendation, that chosen value, and `task:"bounded-convergence-option-b-proof"`.

## Trade-offs and reasoning

Option A stays closest to current streak semantics and preserves more bounded late discovery than C on the descriptive Q-78 replay. Its proposed maximum is `7(m + 2)` review batches and `34(m + 2)` automated-agent calls, where `m` is the work-loop count. Its five-normal-plus-two-reserve values remain weakly calibrated and its account model is substantial.

Selected Option B freezes a finite canonical obligation set `O` and exact phase owners after a separately sealed plan review. Let `n_q = |O_q|` be the count owned by post-freeze phase `q`, and let `r_plan` be the inherited plan-review clean-batch count, one at low risk and two at risky. Its proposed post-freeze phase minimum over clean, ordinarily delivering family paths that eventually construct `Complete` is `L_q = n_q + 1`; its maximum is `4n_q + 1`. Its proposed whole-family minimum over clean, ordinarily delivering paths whose final state is `Complete` is `L_B = r_plan + |O| + m + 1`; its maximum is `4|O| + m + 8`, and its automated-agent maximum is `18|O| + 4m + 38`. Legal early-terminal and non-delivery paths may be shorter and lie outside both minimum domains. It gives scope, obligation ownership, finding disposition, phase authority, and completion one structured architecture. Its costs are the inherited sealed plan-review account, weakly calibrated values, largest schema, clean cost that scales with `|O|`, difficult legacy adoption, and obligation-completeness risk. Under the explicit counterfactual assumption that each of four observed Q-78 folds changes the structured digest, it needs four terminal replans and four additional plan-review and freeze campaigns. This conditional replay is not proof.

Option C has the smallest fixed graph and lowest fixed per-phase maximum. Its proposed maximum is `4(m + 2)` review batches and `15(m + 2)` automated-agent calls. On the descriptive Q-78 replay it stops at pass three and leaves 23 later shortfalls undiscovered, including further highs.

The Option B reasoning is architectural and does not rely on prototype graph size or zero counters. Prefer the cleaner long-term architecture over the smallest diff supports one structured authority model for scope, ownership, disposition, phase authority, and completion. Make illegal states unrepresentable supports exact frozen obligations and owners. Structured data first, project for humans supports one typed source. Minimal by default, Safe on existing projects, and Ground decisions in evidence keep confidence low because B has the largest authoring cost, incomplete obligations create a safety risk, and every adversarial prototype round found serious omissions. Those costs require the blocking proof rather than weakening the choice into implementation authority.

## Prototype boundary

Round 5 established unsoundness around persistent arbitrary-finite finding maps, serious carry across families, products of scope and dismissal re-checks, global family identity, fresh evidence, and combined fold extraction. Earlier rounds exposed cross-owner batches, owner-state products, legacy adoption, unowned in-scope findings, blind closure, reserve and foreclosure semantics, terminal floors, scope routing, successor receipts, and cost definitions.

No proof may treat the prototypes' exact graph counts, zero-valued controls, or conditional replay totals as evidence that Option B is safe. The selected proof must establish every premise of the chosen formula.

## Blocking proof gate

This fold creates `bounded-convergence-option-b-proof` as a separate risky Roadmap unit with no production dependency. It must model selected Option B state, sealed plan review, finite frozen obligations, exact phase ownership, arbitrary finite finding maps, atomic cross-owner and mixed products, serious carry across successors, global append-only family identity, materially fresh evidence, legacy adoption, both terminal floors, every terminal choice, every selected transition, and every algebraic bound. Its blind-closure model composes owner, scope, and dismissal axes: low backlog, medium terminal scope decision, high pending/upheld/overturned scope re-check with overturned high taking its applicable owner route, critical `SeriousBlocked` on either result, closure pending for either scope or dismissal re-check, and atomic scope-plus-dismissal fixtures and mutations.

The proof must include exact reproducible commands that resolve Python through the repository lock, a traceability matrix for every valid verdict in `docs/plans/agent-scaffold.reviews/q86-synthesis-r1-triage.md` through `q86-synthesis-r5-triage.md`, named executable assertions, manifest-resolved named mutations whose results are exercised and killed, and mutation controls for every load-bearing transition and predicate. Each inapplicable finding needs a cited Option-B-specific reason. Planning and documentation findings need corrected durable-text pointers. The proof artefact must converge as risky work before implementation starts.

If the proof fails, implementation remains blocked and Q-86 returns to the human with the new evidence. The human may revise Option B, choose A or C and commission that architecture's proof, or abandon the redesign. No fallback architecture, floor, bound, or implementation unit is automatic.

## Terminal floor

Q-91 separately records the exact floor options `high`, `critical`, and `defer`, with recommendation and chosen value `high`. Under selected floor `high`, ordinary residual acceptance and ordinary narrowing are unavailable while a high or critical is open. The proof must model both `high` and `critical` so their safety difference remains executable, but only `high` is selected.

## Authority boundary

Q-86 and Q-91 authorise only the proof-of-concept unit. The current resettable mechanism remains non-admissible comparison evidence and the severity-decay gate remains excluded. No controller constant, current cap, acceptance rule, reset behaviour, pack change, production documentation, code change, or production implementation is approved. Every later production implementation unit for bounded convergence must use the typed unwaived complete-only proof blocker planned in `workflow-ready-frontier-scheduler`; production readiness requires proof status `complete`, round-backed convergence at the declared risky bar, and no step or increment convergence waiver. `Skipped`, every other non-`complete` proof state, roundless or below-streak `complete`, and waiver-backed `complete` remain blocking, while ordinary blockers and generic convergence waivers retain their existing semantics. No production unit exists yet. Q-92 later fixed this gate requirement and records only the capped proof-plan closure path; it changes neither the Q-86 choice nor this no-production authority.

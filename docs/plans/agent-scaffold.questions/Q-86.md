The corrected decision-ready synthesis is `docs/plans/workflow-calibration.explorations/Q-86-synthesis.md`. Q-86 remains `open` for two human choices through the human-input contract.

- Choose `A - Sealed phase budget`, `B - Frozen obligations with sealed phase campaigns`, or `C - Fixed-depth verification protocol`.
- Choose the serious terminal floor `high` or `critical`, or explicitly defer that floor to a separate decision before implementation.

The synthesis recommends B at medium to low confidence because its corrected `FrozenObligationCampaign` proof combines a non-resettable authority envelope with a finite structured completion predicate. Its whole-family review bound is `4|O| + m + 8`, not the withdrawn `2|O| + 8`. The Q-78 scope replay also shows B's cost: four terminal replans, four additional human receipts, and four additional plan-review and freeze campaigns. A would have stopped at pass seven with three later low shortfalls undiscovered. C would have stopped at pass three with 23 later shortfalls undiscovered, including highs.

The recommended floor is `high`. It removes ordinary residual acceptance and ordinary narrowing while a high or critical is open. The mandatory minimum is `critical`, which preserves more human discretion but permits residual acceptance of a high. Neither floor is selected yet.

The current mechanism remains non-admissible comparison evidence. The severity-decay candidate remains excluded until prospective trajectory evidence is trustworthy. A failed B schema and reconstruction proof of concept must return to the human with A as the recommendation rather than selecting A automatically. Q-85 approved only the design pass. No mechanism, floor, constant, pack change, or implementation is approved until the human decisions are receipted and folded into `workflow-calibration`.

The corrected decision-ready synthesis is `docs/plans/workflow-calibration.explorations/Q-86-synthesis.md`. Q-86 remains `open` for two human choices through the human-input contract.

- Choose `A - Sealed phase budget`, `B - Frozen obligations with sealed phase campaigns`, or `C - Fixed-depth verification protocol`.
- Choose the serious terminal floor `high` or `critical`, or explicitly defer that floor to a separate decision before implementation.

The synthesis recommends B at medium to low confidence because its corrected complete-finding-map proof combines a non-resettable authority envelope with a finite structured completion predicate. Each B phase has a clean minimum of `n_q + 1` review batches and a maximum of `4n_q + 1`. Its whole-family minimum is `r_plan + |O| + m + 1`, and its maximum is `4|O| + m + 8`. B therefore has ordinary review cost that scales with the future frozen obligation count, unlike A and C. Its phase factor is enumerated, while the family bound follows by exact-owner summation with no transfer.

The Q-78 log observes four named folds but carries no prospective B obligation digests. Under the explicit assumption that those folds each change the structured obligation or exclusion digest, B would require four terminal replans, four additional human receipts, and four additional plan-review and freeze campaigns. Those costs are conditional, not reconstructed historical facts. A would stop at pass seven with three later low shortfalls undiscovered. C would stop at pass three with 23 later shortfalls undiscovered, including highs.

The corrected proof exhausts complete stable finding maps to cardinality two across all four severities, including low plus critical and parent plus fix-induced child. Joint repair and verification cover the complete selected set, and delivery universally quantifies over the map. The arbitrary finite result composes because transitions union stable ids and update dispositions pointwise, while adding a finding cannot erase another or weaken release. The proof also controls A's upheld-dismissal reserve path, B's `LegacyNoRubric`, initial-attempt priority, `UnownedInScopeFinding`, untested residual delivery, and materially-different successor receipts.

The recommended floor is `high`. It removes ordinary residual acceptance and ordinary narrowing while a high or critical is open. The mandatory minimum is `critical`, which preserves more human discretion but permits residual acceptance of a high. Neither floor is selected yet.

The current mechanism remains non-admissible comparison evidence. The severity-decay candidate remains excluded until prospective trajectory evidence is trustworthy. A failed B schema and reconstruction proof of concept must return to the human with A as the recommendation rather than selecting A automatically. Q-85 approved only the design pass. No mechanism, floor, controller constant including A's five-plus-two, B's one reopen, or C's four-stage and two-repair depth, pack change, or implementation is approved until the human decisions are receipted and folded into `workflow-calibration`.

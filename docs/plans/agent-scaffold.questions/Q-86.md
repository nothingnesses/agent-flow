The narrowed decision artefact is `docs/plans/workflow-calibration.explorations/Q-86-synthesis.md`. Q-86 remains `open` for an architecture choice and a serious terminal-floor choice through the human-input contract.

Q-88 ended the five-round capped proof-before-choice artefact. The three architecture proposals and their algebraic finite bounds remain available, but `q86-controller-proof.py` and `q86-q78-scope-replay.sh` are adversarial prototypes rather than recommendation-eligibility proofs. The current workflow and code remain unchanged.

## Architecture options

1. `A - Sealed phase budget`. Proposed maximum `7(m + 2)` review batches and `34(m + 2)` automated-agent calls. It stays closest to current streak semantics and preserves more bounded late discovery than C on the descriptive Q-78 replay. Its five-normal-plus-two-reserve values are weakly calibrated and its account model is substantial.
2. `B - Frozen obligations with sealed phase campaigns`. Proposed clean post-freeze phase minimum `n_q + 1`, maximum `4n_q + 1`, whole-family minimum `r_plan + |O| + m + 1`, whole-family maximum `4|O| + m + 8`, and automated-agent maximum `18|O| + 4m + 38`. It gives scope, obligation ownership, finding disposition, phase authority, and completion one structured architecture. It also has the largest schema, clean cost that scales with `|O|`, difficult legacy adoption, and an obligation-completeness risk.
3. `C - Fixed-depth verification protocol`. Proposed maximum `4(m + 2)` review batches and `15(m + 2)` automated-agent calls. It has the smallest fixed graph and lowest fixed per-phase maximum, but sends late findings to a terminal choice earlier. On the descriptive Q-78 replay it stops at pass three and leaves 23 later shortfalls undiscovered, including further highs.

## Recommendation and confidence

The provisional recommendation remains `B - Frozen obligations with sealed phase campaigns`, at low confidence. This recommendation rests only on the conceptual architecture and its alignment with Prefer the cleaner long-term architecture over the smallest diff, Make illegal states unrepresentable, and Structured data first, project for humans. It does not rely on a zero counter or graph size from the broken prototype.

Confidence is low under Minimal by default, Safe on existing projects, and Ground decisions in evidence. B has the greatest authoring cost, its clean review volume scales with the future obligation count, an incomplete obligation set creates a new safety risk, and the adversarial prototype review found serious omissions in every round. The human may choose A, B, or C. The recommendation selects nothing.

## Prototype boundary

The retained scripts exposed proof obligations rather than closing them. Round 5 established unsoundness around persistent arbitrary-finite finding maps, serious carry across families, products of scope and dismissal re-checks, global family identity, fresh evidence, and combined fold extraction. Earlier rounds exposed additional obligations around cross-owner batches, owner-state products, legacy adoption, unowned in-scope findings, blind closure, reserve and foreclosure semantics, terminal floors, scope routing, successor receipts, and cost definitions.

No human-facing decision should treat the prototype's exact graph counts, zero-valued controls, or conditional replay totals as proof that a controller is safe or recommendation-eligible. The algebraic finite bounds are proposed consequences of each architecture's finite topology. The selected-option proof of concept must establish the premises that make the chosen formula valid.

## Blocking selected-option proof gate

After the human chooses A, B, or C, the later planner fold must materialise one separate reviewed executable proof-of-concept Roadmap unit for that architecture. Its completion is an explicit blocker for every production implementation unit. It must model arbitrary finite finding maps soundly, atomic cross-owner batches, cross-family serious carry, scope and dismissal products, global append-only family identity, materially fresh evidence, legacy adoption, both terminal floors, and every selected-option transition and bound.

The proof must include a traceability matrix covering every valid verdict in `docs/plans/agent-scaffold.reviews/q86-synthesis-r1-triage.md` through `q86-synthesis-r5-triage.md`. Each applicable finding needs a named executable assertion and mutation control. An inapplicable finding needs a cited architecture-specific reason. Planning and documentation findings need pointers to corrected durable text. The proof artefact itself must converge as risky work before implementation starts.

If the selected proof fails, implementation remains blocked and Q-86 returns to the human with the new evidence. The human may revise the architecture, choose another architecture and commission its proof, or abandon the redesign. No fallback architecture or terminal floor is automatic.

## Serious terminal-floor co-decision

- Choose `high`, recommended. Ordinary residual acceptance and ordinary narrowing are unavailable while a high or critical is open.
- Choose `critical`, the mandatory minimum. An open critical blocks those choices, while a high may be accepted as residual risk.
- Choose `defer`. A separate receipted floor decision is then required before the selected proof can close and before implementation.

The selected proof must model both floor values even if the human selects one.

## No-decision boundary

The current resettable mechanism remains non-admissible comparison evidence. The severity-decay gate remains excluded. Q-85 authorised the design pass, and Q-88 authorised the narrowed proof timing. No architecture, serious floor, controller constant, current cap, acceptance rule, reset behaviour, pack change, or code change is approved. The human architecture choice authorises only the selected architecture's proof of concept. Production implementation remains blocked until that proof closes every applicable valid Q-86 synthesis finding.

The tracked investigation brief is `docs/plans/workflow-calibration.explorations/Q-86-convergence-mechanism-brief.md`. Q-86 remains `exploring`; the current mechanism is a required but explicitly non-admissible comparison baseline, while more than one viable bounded mechanism must pass the finite-bound or exact-terminal-event proof and red controls before becoming recommendation-eligible. The later explorer proposals and synthesis must produce that bounded option set before the human chooses a mechanism.

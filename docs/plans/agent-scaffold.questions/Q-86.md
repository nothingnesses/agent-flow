The completed decision-ready synthesis is `docs/plans/workflow-calibration.explorations/Q-86-synthesis.md`. Q-86 is now `open` for the human to choose exactly one viable bounded architecture through the human-input contract.

- `A - Sealed phase budget` preserves triage-backed clean-streak semantics inside fixed non-resettable phase slices, at the cost of a larger account model and uncalibrated numeric values.
- `B - Frozen obligations with a sealed derived budget` freezes a finite obligation set and stops on its closure inside a non-resettable derived ceiling. The synthesis recommends it because it addresses both the formal reset problem and the measured moving-scope problem, at the cost of the largest authoring and migration burden.
- `C - Fixed-depth verification protocol` uses a four-stage forward-only discovery, informed-verification, and blind-closure graph, at the cost of terminally rejecting more useful late repairs.

The current mechanism remains non-admissible comparison evidence, and the severity-decay candidate remains excluded until its instrumentation prerequisite is repaired. Q-85 approved only the design pass. No mechanism or implementation is approved, and implementation remains blocked until the human choice is receipted and folded into `workflow-calibration`.

Q-90 records how the human closed the narrowed Q-86 decision artefact after the Q-89 focused verification found one valid low current-state wording defect.

## Decision

Q-89 retired the capped narrowed counters and authorised focused mixed-model verification of the two low repairs. That verification found one valid low defect: the structured Q-86 ask said the artefact had already ended through the verification while also making closure conditional on its result. The finding returned to the human as Q-89 required.

The five presented options, in order, were:

1. `Fix and merge`.
2. `Fix and verify again`.
3. `Accept the wording defect`.
4. `Revert the narrowed synthesis`.
5. `Abandon the redesign`.

The recommendation was `Fix and merge`. The human chose `Fix and merge` on 2026-08-28.

## Trade-offs

- `Fix and merge` makes the exact current-state wording correction, merges the planning fold, and closes the narrowed decision artefact without another review. It removes the reproduced contradiction at the lowest further process cost.
- `Fix and verify again` would make the same correction and run another focused verification. It adds independent evidence but extends a capped artefact after the human has already received and decided the focused verification result.
- `Accept the wording defect` would close immediately but leave the structured source saying the artefact both ended through the verification and remained conditionally open.
- `Revert the narrowed synthesis` would discard the bounded options, algebraic bounds, and selected-option proof gate and return Q-86 to exploration because of one low wording defect.
- `Abandon the redesign` would stop further cost and leave the current workflow unchanged, but it would also leave the known unbounded resume and acceptance paths without a planned replacement.

## Judgement against every Project Principle

- **Prefer the cleaner long-term architecture over the smallest diff.** Correcting the structured source before closure preserves one internally coherent account of the artefact state.
- **Minimal by default.** One exact wording correction with no further review is proportionate to one reproduced low defect.
- **Safe on existing projects.** The correction changes planning records only; the current workflow and code remain unchanged.
- **Idempotent.** One terminal closure record avoids resetting the retired counters or starting another loop.
- **Make illegal states unrepresentable.** The corrected ordering cannot represent the artefact as both already ended and still conditionally open.
- **Ground decisions in evidence.** The focused verification and triage reproduced the defect and identified the exact correction.
- **Reproducible.** The verification finding, correction, and exact receipt remain durable evidence of the closure path.
- **Structured data first, project for humans.** The structured Q-90 question and exact JSONL receipt are the decision source, while this sidecar and the owning step explain the result to readers.

## Fold

Q-90 is `decided -> folded into workflow-calibration`. It authorised only the exact Q-86 current-state wording correction and merge, with no further review round. At that decision point the narrowed decision artefact closed while Q-86 stayed `open`. Q-90 selected no architecture or serious terminal floor and authorised no workflow, pack, or code implementation. Q-86 and Q-91 later made those separate proof-only decisions.

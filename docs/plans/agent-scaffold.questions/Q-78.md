Decision 26 (human, 2026-08-23; receipt `Q-78-intent-paragraphs`) revises the intent-value shape. `problem` and `approach` remain two separate required, non-empty fields, but each logical value may contain one or more paragraphs. A single paragraph is valid. TOML multiline basic strings, multiline literal strings and escaped newline values are supported. `render`, `next` and `status --step` preserve paragraph boundaries under deterministic labels, and no sentence, line or character cap applies.

This sub-decision is folded into `step-intent-encoding` and supersedes that step's earlier single-line, one-sentence clauses and their physical-line cost rationale. It does not supersede `Q-78-requiredfields`, the two-field choice, the migration record outside the plan, the per-field citations and marks, or the boundary between intent and decision evidence. The accepted trade-off is that paragraph values can make `[[step]]` blocks and reorder diffs larger.

`Q-78` remains `open`: decision 26 revises one part of the design under review and does not close the whole question. This non-empty body also supersedes the question ask's historical observation that every question sidecar is empty; the TOML ask remains the structured queue item, and this sidecar is supplemental decision detail.

Q-92 records how the human closed the capped Q-86/Q-91 proof-plan decision fold.

## Decision

The proof-plan decision fold reached its five-round cap with a clean streak of zero of two. Round 5 returned four valid findings, one high and three low. The human chose the specific-fix closure path through the human-input contract.

The five presented options, in order, were:

1. `Fix, verify once, then close`.
2. `Resume the full review loop`.
3. `Accept the current plan`.
4. `Revert the proof-plan fold`.
5. `Abandon the redesign`.

The recommendation was `Fix, verify once, then close`. The human chose `Fix, verify once, then close` on 2026-08-30.

## Trade-offs

- `Fix, verify once, then close` applies only the four corrections adjudicated in `docs/plans/agent-scaffold.reviews/q86-decision-r5-triage.md`, then runs one focused GPT-and-Claude verification over those fixes and their direct residue. If it finds no new valid finding, the capped decision fold closes without a second clean round. Any new valid finding returns to the human. This checks the specific high-risk proof-gate repair independently without resetting a full risky loop, but it closes the planning artefact below its ordinary two-clean-round bar.
- `Resume the full review loop` would reset the counters and return to the ordinary risky requirement of two consecutive clean rounds. It preserves the normal convergence bar but can start another long plan-review campaign after five rounds already identified the exact remaining fixes.
- `Accept the current plan` would close immediately with the valid convergence-waiver bypass, traceability gap, stale scheduler comment, and post-freeze replay gap. It costs no more review but leaves a high-severity path by which an unproved proof could authorise future production.
- `Revert the proof-plan fold` would remove the Q-86/Q-91 proof plan and return the architecture decision to its pre-fold state. It removes the defective gate but discards the reviewed Option B proof obligations and leaves no selected proof path.
- `Abandon the redesign` would stop further cost and leave the current workflow unchanged. It also leaves the known unbounded convergence problem without a planned replacement.

## Specific fix and focused verification

The authorised fix is exactly the four valid round-5 corrections:

1. Future bounded-convergence production readiness requires the proof target to have status `complete`, round-backed convergence at its declared risky bar, and no step or increment convergence waiver. The generic waiver model remains available to ordinary steps.
2. Every executable traceability row's named mutation resolves against the mutation manifest and has an exercised-and-killed result; unknown, surviving, or unexercised names fail.
3. The aligned scheduler pseudocode states the ordinary complete-or-skipped blocker rule and its unwaived complete-only exception without contradiction.
4. Post-freeze acceptance and mutation controls replay identical ordered events and compare architecture, floor, family id, spend, authority, finding map, owner routes, and terminal state across resume, rename, rebuild, and unchanged replan, with no replenishment.

The capped counters retire rather than reset. One focused GPT-and-Claude verification checks only these fixes and their direct residue. If it reports no new valid finding, the decision fold closes and merges without a second clean round. Any new valid finding returns to the human. This below-streak closure applies only to the capped planning artefact; it is not a convergence waiver for the future proof increment, which must still achieve its two round-backed clean rounds.

## Judgement against every Project Principle

- **Prefer the cleaner long-term architecture over the smallest diff.** Repairing the gate at its typed scheduler and workflow-validation boundary is cleaner than accepting a status-only local exception.
- **Minimal by default.** One focused verification is proportionate to four fixed findings and avoids resetting the entire plan-review loop.
- **Safe on existing projects.** The repairs change planning records only, leave the current workflow and code unchanged, and keep production blocked.
- **Idempotent.** The capped counters retire once under one durable closure path rather than repeatedly resetting the same artefact.
- **Make illegal states unrepresentable.** The production-readiness predicate cannot be satisfied by any convergence waiver or non-complete proof state.
- **Ground decisions in evidence.** The closure applies exactly the four reproduced corrections adjudicated by the round-5 triage before accepting the plan.
- **Reproducible.** Manifest-resolved mutation results, identical ordered-event replay, and focused independent verification make the correction claims rerunnable.
- **Structured data first, project for humans.** The structured Q-92 question and exact JSONL receipt are the decision source, while this sidecar and the proof step explain the closure path.

## Fold and authority boundary

Q-92 is `decided -> folded into bounded-convergence-option-b-proof`. The proof step is the single `folded_into` owner; the scheduler sidecar carries the enforcement part of the same correction. The exact `type:"decision"` receipt in `docs/metrics/workflow.jsonl` records the five options in order, recommendation and chosen value `Fix, verify once, then close`, `task:"bounded-convergence-option-b-proof"`, and date 2026-08-30.

Q-86 remains Option B and Q-91 remains floor `high`. Q-92 changes only the capped plan-review closure path and the proof plan's safety requirements. It authorises no production behaviour, controller constant, pack change, code change, or production implementation unit.

Q-93 records how the human closed the Q-86/Q-91 proof-plan decision fold after Q-92's focused verification returned two valid low findings.

## Decision

Q-92 retired the capped proof-plan counters and authorised one focused GPT-and-Claude verification of its four fixes and their direct residue. That verification returned two valid low findings in `agent-scaffold.reviews/q86-decision-cap-triage.md`: the traceability command did not make its mutation-result join independently observable, and the scheduler's round-5 provenance pointer was not task-relative. The findings returned to the human as Q-92 required.

The five presented options, in order, were:

1. `Fix and merge`.
2. `Fix and verify again`.
3. `Accept the low residuals`.
4. `Revert the proof-plan fold`.
5. `Abandon the redesign`.

The recommendation was `Fix and merge`. The human chose `Fix and merge` on 2026-08-30.

## Trade-offs

- `Fix and merge` applies exactly the two valid low corrections and merges without another review. The traceability checker imports or invokes the mutation runner against the named manifest in the same command, resolves every executable row's named mutations from that run, reports and asserts resolved, exercised, and killed row counts, and fails unknown, surviving, or unexercised names. The scheduler's round-5 provenance pointer becomes task-relative `agent-scaffold.reviews/q86-decision-r5-triage.md`; pre-existing sibling paths remain unchanged.
- `Fix and verify again` would apply the same two corrections and run another focused verification. It adds independent evidence but extends a capped artefact after the two remaining low defects and their exact fixes are already adjudicated.
- `Accept the low residuals` would merge without either correction. It avoids further edits but leaves the mutation-result join independently unobservable and one durable scheduler evidence pointer unresolved under its documented task-relative convention.
- `Revert the proof-plan fold` would remove the Q-86/Q-91 proof plan and return the architecture decision to its pre-fold state. It removes the residual defects by discarding the reviewed Option B proof obligations and selected proof path.
- `Abandon the redesign` would stop further cost and leave current production unchanged, but it would also leave the known unbounded convergence problem without a planned replacement.

## Judgement against every Project Principle

- **Prefer the cleaner long-term architecture over the smallest diff.** One explicit shared in-process result contract is cleaner than an implicit result handoff between isolated commands.
- **Minimal by default.** Exactly two low corrections and no further review are proportionate to the focused verification result.
- **Safe on existing projects.** The corrections change planning records only; the current workflow and production code remain unchanged.
- **Idempotent.** One terminal closure decision avoids resetting retired counters or starting another verification chain.
- **Make illegal states unrepresentable.** The contract fails every unknown, surviving, or unexercised mutation name and asserts the resolved, exercised, and killed executable-row counts.
- **Ground decisions in evidence.** The two corrections are exactly the defects reproduced and adjudicated in the focused triage.
- **Reproducible.** One invocation names the runner and manifest and exposes the matrix-to-mutation-result join through computed result counts.
- **Structured data first, project for humans.** The structured Q-93 question and exact JSONL receipt are the decision source, while this sidecar and the proof step explain the closure.

## Fold and authority boundary

Q-93 is `decided -> folded into bounded-convergence-option-b-proof`. It authorises exactly the two corrections above and merge without another review. The exact `type:"decision"` receipt in `docs/metrics/workflow.jsonl` records the five options in order, recommendation and chosen value `Fix and merge`, `task:"bounded-convergence-option-b-proof"`, and date 2026-08-30.

Q-86 remains Option B and Q-91 remains floor `high`. The proof step remains `not-started`. Q-93 grants no proof result or convergence credit and authorises no production behaviour, controller constant, pack change, code change, or production implementation unit. Bounded-convergence production implementation remains blocked until the proof is complete, directly converged at its risky bar, waiver-free under the typed gate, and separately authorised.

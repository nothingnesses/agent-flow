Q-89 records how the human closed the capped narrowed Q-86 decision artefact.

## Decision

The narrowed Q-86 artefact reached its five-round cap with a clean streak of zero of two. Narrowed round 5 returned two valid low findings. The human chose how to close that capped artefact through the human-input contract.

The five presented options, in order, were:

1. `Fix, verify once, then close`.
2. `Resume the full review loop`.
3. `Accept the current artifact`.
4. `Revert the narrowed synthesis`.
5. `Abandon the redesign`.

The recommendation was `Fix, verify once, then close`. The human chose `Fix, verify once, then close` on 2026-08-28.

## Trade-offs

- `Fix, verify once, then close` repairs the two reproduced low defects and runs one focused mixed-model verification over those fixes and their direct residue. If it finds nothing new, the capped narrowed artefact closes without a second clean round. Any new valid finding returns to the human. This is cheaper than resetting the full loop while still checking the known repairs independently.
- `Resume the full review loop` would reset the counters and return to the ordinary risky convergence requirement of two consecutive clean rounds. It preserves the normal bar but can reopen a long review campaign for two low documentation defects after ten already-completed Q-86 review rounds.
- `Accept the current artifact` would close immediately with the two valid low defects still present: the serious-floor rationale uses names outside the plan's Project Principles, and Option C's fifteen-call maximum lacks a local derivation.
- `Revert the narrowed synthesis` would discard the bounded options, algebraic bounds, and selected-option proof gate and return Q-86 to exploration. It abandons useful decision material because of two local documentation defects.
- `Abandon the redesign` would stop further cost and leave the current workflow unchanged. It would also leave the known unbounded resume and acceptance paths without a planned replacement.

## Judgement against every Project Principle

- **Prefer the cleaner long-term architecture over the smallest diff.** Repairing the two known defects is cleaner than accepting an internally inconsistent decision artefact.
- **Minimal by default.** One focused verification is proportionate to two low documentation fixes and avoids reopening the full loop.
- **Safe on existing projects.** The repair changes planning records only; the current workflow and code remain unchanged.
- **Idempotent.** The capped counters retire once under one durable closure decision rather than repeatedly resetting the same artefact.
- **Make illegal states unrepresentable.** Closure is conditional on verification finding nothing new, and any new valid finding has only the route back to the human.
- **Ground decisions in evidence.** The two reproduced round-5 defects are repaired before closure rather than waived without evidence.
- **Reproducible.** A focused mixed-model verification checks the named fixes and their direct residue against the durable round-5 triage.
- **Structured data first, project for humans.** The structured Q-89 question and exact JSONL receipt are the source of the decision, while this sidecar and the owning step explain it to readers.

## Fold

Q-89 is `decided -> folded into workflow-calibration`. The capped narrowed counters retire rather than reset. After the two fixes, one focused mixed-model verification closes the capped narrowed artefact if it finds nothing new; any new valid finding returns to the human. Q-86 stays `open`. No architecture or serious terminal floor is selected, and no workflow, pack, or code change is authorised.

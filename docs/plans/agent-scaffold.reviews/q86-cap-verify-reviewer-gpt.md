# Focused Q-86 cap verification — GPT

Reviewed tip `1419a2de` only against the two narrowed-round-5 low fixes, their direct residue, and Q-89's authorised specific-fix closure path. I read `AGENTS.md`, Q-89 and its JSONL receipt, `q86-narrow-r5-triage.md`, the corrected Q-86 synthesis, Q-86 sidecar and structured ask, `workflow-calibration`, Success Criteria, and the generated projection. I respected the deferred selected-option proof gate and did not reopen or test it.

The two known low defects are repaired correctly in the decision synthesis: the serious-floor recommendation now uses applicable plan Principles (**Make illegal states unrepresentable**, **Safe on existing projects**, **Ground decisions in evidence**, and **Minimal by default**) and states both its fail-closed safety case and its evidence/minimality cost; Option C locally derives fifteen automated-agent calls as five reviewer passes, four triage calls, four backstop calls, and two repair passes (`5 + 4 + 4 + 2 = 15`). No stale occurrence of the former out-of-plan Principle names remains on the Q-86 live surfaces. Q-89's five option labels, order, recommendation, chosen value, task, and receipt pointer match exactly, and its specific-fix branch retires rather than resets the capped counters, closes after one focused mixed-model verification only if nothing new is found, and returns any new valid finding to the human. Strict render and workflow validation pass with 492 valid metric records, 114 steps, 89 questions, and workflow invariants holding.

## Finding 1 — the structured Q-86 ask says the artefact has ended through a verification that it simultaneously says is still conditional

- **Severity:** low.
- **Evidence:** The authoritative structured ask says, “Q-89 ended the capped narrowed artefact through two low fixes and one focused mixed-model verification; if that verification finds nothing new, the capped narrowed artefact closes” (`docs/plans/agent-scaffold.plan.toml:2562`; projected at `docs/plans/agent-scaffold.md:179`). The first clause presents the focused verification and artefact ending as accomplished, while the second makes closure conditional on that verification's result. The corrected synthesis instead states the intended order unambiguously: Q-89 records the choice, the counters retire, the verification checks the landed fixes, and only a no-new-finding result closes the artefact (`docs/plans/workflow-calibration.explorations/Q-86-synthesis.md:7`). The Q-86 sidecar says the same (`docs/plans/agent-scaffold.questions/Q-86.md:3`).
- **Why this is new and in scope:** This wording was introduced by the Q-89 fold, not raised or settled by narrowed round 5. It is direct residue of the specific-fix closure path, not a deferred-proof claim. It can misstate the current state at the exact human-facing structured source that the generated queue projects.
- **Correction:** Replace the structured ask's “Q-89 ended ... through” clause with the ordering already used by the synthesis and sidecar, for example: “Q-89 chose `Fix, verify once, then close`: after the two low fixes, one focused mixed-model verification closes the capped narrowed artefact if it finds nothing new; any new valid finding returns to the human.” Re-render the projection.

## Counts

Critical: 0. High: 0. Medium: 0. Low: 1. **Total: 1.**

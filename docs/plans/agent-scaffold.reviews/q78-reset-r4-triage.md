# Q-78 reset round 4 triage

## Method and gates

I read the committed brief, both merged round-4 reviewer files, the ledger from `RESUME HERE`, the round-3 triage and repair brief, the current plan source, the active step sidecars, and the relevant question sidecars. I did not read a reviewer fixture. Every reconstructed artifact is under the authorised child directory `/tmp/claude-1000/-home-jessea-Documents-projects-agent-scaffold/2fed83bd-4a13-402b-9e76-143356c0d130/scratchpad/q78-reset-r4-triage/triager`.

All toolchain commands used the project direnv development shell. The baseline gates passed:

- source-plus-metrics validation: `445 records, valid`; `105 steps, 81 questions, valid`;
- workflow validation: `workflow invariants hold`;
- strict render: `docs/plans/agent-scaffold.plan.toml: up to date`;
- tests: 470 passed, 0 failed across 12 test binaries;
- `cargo clippy --all-targets --all-features -- -D warnings`: exit 0.

## Raw verdicts

| Raw finding | Verdict | Distinct finding | Owner | Final severity | Final class | Smallest correction |
| --- | --- | --- | --- | --- | --- | --- |
| R4G-1 | valid | I8 | `step-intent-encoding-inc1` | medium | 1 | Make criterion 8 run the full criterion-2 matrix for both fields on both `status --step` surfaces, with explicit outer-whitespace human and interior-whitespace JSON comparisons; add field-specific red mutations. |
| R4G-2 | valid | I9 | `step-intent-encoding-inc1` | low | 1 | Compare the entire generated `beta` fragment beginning immediately after `alpha`, or assert that its first generated byte is `*`; do not begin the checked range at the first `**Problem**`. |
| R4G-3 | valid | I10 | `step-intent-encoding-inc1` | low | 1 | Add an empty-body, field-bearing render unit case and compare its complete Step Details fragment against the required labelled block. |
| R4G-4 | valid | I11 | `step-intent-encoding-inc3` | medium | 1 | Require the named increment-1 projection tests to remain green without weakened expectations, and exercise fresh TOML scaffolds through human and JSON `next` plus TOML `status --step`. |
| C4-1 | valid | I12 | `step-intent-encoding-inc1` | medium | 1 | Repeat criterion 7's human and JSON checks in a non-review state, minimally `awaiting-fixes`, with its complete five-key context and a red mutation that scopes intent insertion to review states. |

No finding is accepted as residual risk.

## Deduplication

No reports describe the same defect. I8 is the missing `status --step` value/surface matrix; I9 is a fabricated generated heading hidden before the selected range; I10 is the empty-body contribution branch; I11 is increment 3's loss of projection-test retention; and I12 is the independent `next` loop-state axis. The raw and distinct counts are therefore both five.

## Reproduced evidence

### I8 — `status --step` misses field/surface matrix coordinates

Rule 10 requires every projection to preserve the stated logical value contract (`docs/plans/agent-scaffold.steps/step-intent-encoding.md:53`), and criterion 2 makes the full matrix the oracle for every projection (`:164-189`). Beyond the ordinary `good` fixture, the status human matrix names repeated blank, interior whitespace, CRLF, and bare CR (`:284-290`). The status JSON checks name `good` and CRLF (`:292-304`); the partial fixtures cover outer-whitespace `problem` and repeated-blank `approach` (`:306-330`).

I independently extracted criterion 8 and checked its literal coordinates with GNU grep. It printed:

```text
human_status_named_matrix=1
human_status_outer_approach_literal=0
status_json_interior_approach_literal=0
partial_problem_outer_literal=1 partial_approach_repeated_literal=1
```

Thus a human-status display path that omits whole-value trimming only for outer-whitespace `approach`, and a status-JSON path that alters interior whitespace only for `approach`, both evade their named status witnesses. The full `next` matrix is a different projection and cannot certify these `status` paths. Each implementation violates Rule 10 while passing the stated criterion, so I8 is class 1.

### I9 — the no-heading range discards fabricated content before `**Problem**`

The no-heading rule requires labelled intent before a body with no heading (`step-intent-encoding.md:98`), but criterion 6 begins its checked range at the first `**Problem**` after alpha (`:242-248`). I created correct and wrong generated fragments in the authorised scratch directory; the wrong fragment inserts `### \`beta\`: fabricated heading` between alpha and `**Problem**`. Running criterion 6's exact `awk` selector on both then `cmp`ing the ranges produced:

```text
range_equal=true fabricated_headings=1 first_range_line=**Problem**
```

The source-side heading count in criterion 6 remains zero because the fabricated heading is generated, not in `beta.md`. The existing selector therefore accepts an output that violates the explicit no-heading rule. I9 is class 1.

### I10 — no field-bearing fixture has an empty body

The render contract explicitly requires a field-bearing step to contribute even when its body is empty (`step-intent-encoding.md:100`). Criterion 4 assigns fields to `alpha`, `beta`, `eta`, and `gamma` (`:202-204`), and criterion 6's partial and no-heading cases also retain bodies (`:228-248`). My independent census of those four actual fixture sidecars printed:

```text
alpha   nonempty
beta    nonempty
eta     nonempty
gamma   nonempty
field_bearing_cases=4 empty_field_bearing_cases=0 wrong_renderer_emits_empty_body_with_fields=0
```

Here `wrong_renderer_emits_empty_body_with_fields=0` is the reconstructed branch that contributes only when `body` is non-empty. It reaches every field-bearing case the criteria name yet omits the required empty-body-with-field case. The present N1 test is deliberately no-field until increment 3 (`:102`), so it does not close this increment-1 hole. I10 is class 1.

### I11 — increment 3 protects validation but not the projection behaviour it rewrites

Increment 3 promises that `render` stays unconditional, TOML `next` wraps both values in `Some`, and TOML `status --step` retains both (`step-intent-encoding.md:673`). Its first criterion only proves the required schema and parse behaviour (`:730-766`). Criterion 2 retains N1 plus the four Rule-2/Rule-3 validation tests (`:768-772`); criterion 8 checks rendered projection totals (`:856-860`); and criterion 12 runs only Markdown `status --step` (`:872-880`).

I independently extracted the twelve acceptance criteria. Their only named retained test family is `validate_(rejects|accepts)_a`; no criterion requires an increment-1 `next` or TOML-status projection test to remain, and no acceptance command invokes `next` on the flipped TOML source. A flip can therefore set TOML `StepInfo` intent to `None` (or omit a context insertion), weaken or remove the increment-1 projection checks, and still satisfy the schema, render, fresh-scaffold, template, changed-path, Markdown-status, and green-suite requirements. That violates Rule 10 and the increment's explicit projection promise, so I11 is class 1.

### I12 — `next`'s intent requirement is untested outside review states

The increment requires `build_context` to insert both intent slots “in every loop state” (`step-intent-encoding.md:112`). Criterion 7 fixes the review-state context, including `review_findings` and `triage_findings` (`:250-282`), but names no second state.

I created independent one-step TOML plans and round logs in the authorised scratch directory, then ran the current binary's JSON projection. It reported:

```text
{"state":"ready-to-plan","ctx":["isolation_tier","ledger"]}
{"state":"awaiting-first-review","ctx":["isolation_tier","ledger","review_findings","triage_findings"]}
{"state":"awaiting-fixes","ctx":["isolation_tier","ledger","triage_findings"]}
{"state":"blocked","ctx":["blocked_by","isolation_tier","ledger"]}
```

The actual state split is in `src/next.rs:993-1020`: review states receive two extra slots, `awaiting-fixes` receives only `triage_findings`, and ready/blocked states differ again. Moving the future two intent inserts inside the review-state match arm leaves criterion 7's six-key review-state tests green but omits the instruction values from `awaiting-fixes`, ready-to-plan, and blocked contexts. That violates the explicit every-state requirement and its instruction-surface risk ground. I12 is class 1, not the class-2 fallback.

## Per-increment outcome

Counts below are round-4 findings. The settled class-2 I2 and I6 findings remain recorded but are not re-raised; neither changes these outcomes.

| Increment | Distinct valid | Severities | Class 1 / class 2 / neither | Outcome | Resulting streak | Converges | Foreclosed |
| --- | ---: | --- | --- | --- | ---: | --- | --- |
| `sidecar-status-opening-drift-inc1` | 0 | none | 0 / 0 / 0 | clean | 2 | yes | no |
| `step-intent-encoding-inc1` | 4 | medium, medium, low, low | 4 / 0 / 0 | new valid | 0 | no | yes |
| `step-intent-encoding-inc3` | 1 | medium | 1 / 0 / 0 | new valid | 0 | no | yes |

All loops are risky and have one round left after this fourth round. For the clean sidecar loop, `1 < (2 - 2)` is false and it converges. For each reset-to-zero loop, `1 < (2 - 0)` is true: a fifth clean round would only reach streak one. `step-intent-encoding-inc1` and `step-intent-encoding-inc3` therefore require the human escalation required by decision 24; no escalation record is appended here.

The viable disposition classes for each escalation are: reset the counters and continue after repair; explicitly accept the residual risk through the required human-recorded close/waiver route; or retire, supersede, or otherwise close the increment rather than continue this loop. I make no disposition choice.

## Totals, backstop, and foreclosure

- Raw findings: 5.
- Raw verdicts: 5 valid, 0 dismissed, 0 accepted risk.
- Distinct valid findings: 5.
- Raw class totals: 5 class 1, 0 class 2, 0 neither.
- Distinct class totals: 5 class 1, 0 class 2, 0 neither.
- Severity ceiling: medium.
- Backstop: none owed. No high or critical finding was dismissed.
- Foreclosure: escalation is owed for `step-intent-encoding-inc1` and `step-intent-encoding-inc3`; `sidecar-status-opening-drift-inc1` converges.

# Q-78 second foreclosure fix brief

Act only as planner. Read AGENTS.md, the second-foreclosure ledger state, round-4 triage and reviewer files, and increment 1.

Fix only GPT-R4-1. Run the complete independent parser-value matrix through both pending constructors, `ReadyToPlan` and `Blocked`, on both human and JSON `next` surfaces, with both fields. Require exact unchanged JSON values and exact human display bytes for every row. Pin complete context keys for both states.

Add pending-transfer-only red controls for paragraph truncation and line-ending or whitespace normalisation. Each must fail both affected pending surfaces while the in-progress matrices and direct all-state seam remain green. Retain the separate absence-only pending mutation.

Do not fix C4-1 or earlier settled class-2 findings. No waiver exists until one post-fix round is clean.

Edit only `docs/plans/agent-scaffold.steps/step-intent-encoding.md` and generated `docs/plans/agent-scaffold.md`. Use direnv. Run validations, render checks, tests, Clippy, diff, ASCII and path checks. Commit with docs subject. Do not push.

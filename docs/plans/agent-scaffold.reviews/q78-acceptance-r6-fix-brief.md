# Q-78 acceptance pass 6 repair brief

Act only as planner. Read AGENTS.md, both pass-6 reports, `q78-acceptance-r6-triage.md`, Q-81/Q-83, current history and affected sidecars.

Record the human drift-size refinement as Q-84, decided and folded into `sidecar-status-opening-drift`, with this exact receipt:

Options:

- `Continue with the measured set of 18`.
- `Retain the original set of 19`.
- `Reopen the drift design`.

Recommendation and chosen: `Continue with the measured set of 18`.

Repair the four planner-owned findings:

1. R6-G1: preserve the thirteen Q-81 plan-review windows in a bounded typed historical Roadmap-increment-plan-review representation. Leave their existing live delivery increments declared `work_review`. Preserve all 41 round phases, classes, identities and escalation segmentation. Do not consume the live declarations, create new delivery ids, rewrite history or weaken phase checks.
2. A: give exact declaration/migration routes to `decision-folder-currency-fold`, `q77-fold` and `step-intent-fold`. Register the two real non-Roadmap tasks as needed. Extend the current-history criterion to cover all 99 plan-review records, with an unregistered-task red control. Reconcile this with the bounded Q-81 history representation.
3. B/Q-84: remeasure the drift selectors and correct the dated evidence to 44/35/27/18. Identify `workflow-driver` as the removed member and record that Q-84 preserves dynamic selectors as implementation authority. Do not make moving counts acceptance conditions.
4. R6-G2: mark the stale Stage-3 ordering and recommendation sentences as pre-Q-82 history. Limit remaining evidence gates to Stages 4 through 6 and preserve write-path and authoritative-driving gates.

The orchestrator already closed R6-G3 by replacing the ledger inventory with a structured-queue pointer. Do not edit the ledger.

Update plan TOML, receipts, generated view, relevant sidecars/architecture and Success Criteria. Preserve all statuses, Q-81/Q-83 and converged identities. Do not implement code, edit pack, rewrite metrics history, close Q-58/Q-78 or invent another decision.

Render and run both validations, strict render, tests, Clippy, checks, diff and ASCII checks. Commit with a docs subject. Do not push.

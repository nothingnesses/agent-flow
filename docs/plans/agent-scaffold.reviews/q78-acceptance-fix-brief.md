# Q-78 acceptance repair and scheduling fold brief

Act only as planner in the isolated `plan/q78-design-pass` worktree. Read AGENTS.md, ledger RESUME HERE, both acceptance reports, `q78-acceptance-triage.md`, the complete Q-78 plan product, Q-58, Q-80, Q-81, `workflow-driver`, and the Q-58 ablation design.

Repair all seven valid acceptance shortfalls exactly as triaged. Do not add a Q-81 prose fold, because triage dismissed that subclaim and the typed ownership already exists.

1. T1: remove only the branch-only duplicate JSONL `type:"waiver"` record for `step-intent-encoding-inc1`. Retain the TOML waiver and escalation evidence. This restores Q-46's event-only log.
2. T2: preserve the decided decision-receipt source route. Make its specification executable with precise receipt identity and a bounded relevance oracle. Do not admit the entire metrics log as relevant. Update every affected rule, criterion and exploration statement consistently.
3. T3: give durable Roadmap ownership to the structured-risk-source decision, foreclosure and cap enforcement, Q-80's class-inheritance decision, and the successor drift work. Close Q-80 without asking again. Use the existing `Q-78-classinherit` receipt as evidence. Do not invent a new human choice. If W4 requires a `Q-80` receipt for a decided fold, use an explicit alias receipt with the existing receipt's exact options, recommendation and chosen value, or a schema-valid supersession that still preserves actionable ownership. Prefer focused steps and dependencies over one mixed step.
4. T4: replace or locally mark as historical the stale single-line, one-sentence Q-78 recommendation and cost. Make the live ask agree with required multi-paragraph logical values.
5. T5: remove fixed receipt totals and stale six-plus-six inventory claims from the live Q-78 ask. The orchestrator already marked the ledger's old confirmation historical. Retain reproducible selectors.
6. T6: add the documentation-impact work for `validate-missing-source-exit`, including the appropriate changelog entry, and widen its exact implementation path set so a correct implementation can update that documentation. Preserve any README exclusion that still reproduces.
7. T7: qualify the exploration and Q-78 pointer as the complete design-residual set, not the complete set of every accepted residual in the pass.

Fold the human interrupt decisions into the plan.

Q-58 refinement, options presented:

- `Reopen Q-58 and run the ablation`.
- `Schedule a compact projection directly`.
- `Keep Q-58 deferred`.

Recommendation and chosen: `Reopen Q-58 and run the ablation`. Append a `type:"decision"` receipt under `q_id:"Q-58"`. Keep Q-58 `exploring`, but replace the shelved-current instruction with an owed run of the existing ablation because `next` now emits 727754 bytes. Point to the existing design artifact. The output experiment must decide the compact carrier before implementation. Update `resume-state-currency-signal` gating so it follows the reopened experiment rather than a stale deferred premise.

Create Q-82 for the loop work, options presented:

- `Schedule visibility and scheduling in stages`.
- `Schedule visibility and phase correctness only`.
- `Schedule one combined driver overhaul`.

Recommendation and chosen: `Schedule visibility and scheduling in stages`. Append its decision receipt. Author focused Roadmap ownership and sidecars for:

- A first stage that makes `next` distinguish exploration from review and project every active loop while preserving one selected next action.
- A later reviewed stage that builds the typed fleet and then the ready-frontier scheduler for parallel work, using the existing `workflow-driver` Stage 2 and Stage 3 architecture instead of duplicating it.

Keep the correctness and visibility repair independent of scheduler authority. Record explicit dependencies and future implementation criteria. Do not implement code, edit `pack/`, or change any existing Roadmap step status. Change question status only where Q-80 closes and Q-82 records the new decision. Q-58 remains `exploring`. Add or revise Success Criteria for this expanded plan scope.

Assess documentation impact for every new step. Keep Q-78 open until the later acceptance pass. Preserve all settled residuals and every converged increment identity.

Edit only plan TOML, generated plan view, relevant question, step, exploration and Success Criteria sidecars, and append-only decision receipts, apart from the authorised deletion of the one inert waiver record. Render the plan.

Run both validation modes, strict render check, tests, Clippy, diff checks, ASCII checks and an exact changed-path review. Commit with a docs subject. Do not push. If any instruction conflicts with the schema or a recorded decision, stop and report the exact conflict instead of inventing a workaround.

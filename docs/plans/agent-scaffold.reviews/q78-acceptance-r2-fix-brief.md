# Q-78 acceptance pass 2 repair brief

Act only as planner in the isolated plan worktree. Read AGENTS.md, both pass-2 reports, `q78-acceptance-r2-triage.md`, the current plan, the Q-58 design, Q-82, `workflow-driver` and every affected sidecar.

Repair the nine planner-owned valid findings. R2-G4 is already closed by the orchestrator's current ledger anchor. Do not edit the ledger.

1. R2-G1: replace the impossible step-only phase carrier with a typed carrier that represents legal task-scoped plan review and acceptance as well as step-scoped exploration, implementation and work review. State its structured source, the authorised actor that advances it, legal transitions, and how `next` projects all active units plus one selected action. Test each phase from a workflow-legal state. Do not fake task phases by changing an implementation step's status.
2. R2-G2: make `workflow-driver-typed-fleet` depend on `review-loop-foreclosure-enforcement`. Make it consume and extend the earlier reviewed reconstruction rather than extracting duplicate streak/cap arithmetic.
3. R2-G3: require a committed pre-run Q-58 protocol that fixes the non-inferiority or equivalence margin, confidence level, interval or test method, state aggregation and weighting, cross-model aggregation, and disagreement rule before any result is read. Make the carrier choice mechanically reproducible from retained aggregates. Preserve the oracle-circularity boundary.
4. R2-G5: update the canonical `workflow-driver` status and staging prose. State accurately that stages 0a, 0b and 1 are declared or complete as the TOML records. State how Q-82 replaces the former Stage-3 scheduler evidence gate. Keep detailed criteria in focused child sidecars.
5. R2-C1: make the future over-cap rule satisfiable on current history without deleting or rewriting history. Reproduce `q78-design-pass`'s historical over-cap segment and zero escalation records. Define an explicit, justified treatment for historical or non-Roadmap loops, or append only evidence-backed events if their position can actually segment the historical records. Do not invent events, insert them out of append order, or claim a boundary that the reconstruction cannot consume. Prefer an explicit forward adoption boundary or typed historical exemption if that is the only honest design. Keep future cap enforcement fail-loud.
6. R2-C2: add focused Roadmap ownership for correcting TOML-primary waiver guidance through the canonical `pack/instrument.md` source and its generated copies. The planned implementation must say TOML-primary waivers live as nested `[[step.waiver]]`; JSONL waiver records are legacy Markdown-primary only. Do not edit `pack/` in this design repair.
7. R2-C3: single-source the historical output measurement with reproducible provenance. The original measurement ran at main commit `3ad7b2b` from the repository root with `cargo run -- next --source docs/plans/agent-scaffold.plan.toml --metrics docs/metrics/workflow.jsonl --isolation-tier container`, redirected to a file and measured by `wc -l -w -c`, yielding 1434 lines, 113409 words and 727754 bytes. Name the commit and command at one source, and make other sites reference it or use a bounded historical statement. Keep the ablation's mandatory fresh pre-run measurement.
8. R2-C4: reconcile the structured-risk exact path count with its enumeration. It names seven guidance or template files plus `CHANGELOG.md`, eight total.
9. R2-C5: replace the predecessor's future-tense handover with the authored `sidecar-status-authored-openings` slug while preserving why an early stub was refused.

Update Success Criteria where the corrected carrier, protocol, cap-adoption boundary or waiver-guidance owner changes the expanded acceptance contract. Add dependencies and documentation impact. Do not implement code, edit pack, change existing Roadmap statuses, close Q-58 or Q-78, alter settled residuals, or change converged increment identities.

Edit only plan TOML, generated plan view, relevant step/question/exploration/Success Criteria sidecars, and metrics only if the corrected plan genuinely requires a new already-made decision receipt. No human decision was made in this repair, so do not invent one.

Render. Run both validations, strict render check, tests, Clippy, diff checks and ASCII checks. Commit with a docs subject. Do not push. If a finding requires a new human choice rather than a faithful specification repair, stop and report the choice instead of deciding it.

# Q-78 acceptance pass 5 repair brief

Act only as planner. Read AGENTS.md, both pass-5 reports, `q78-acceptance-r5-triage.md`, Q-83, current history and the driver architecture.

Repair all five findings without a new human decision:

1. R5-1: specify a phase-preserving declaration and reconstruction mapping for every Roadmap-resolving `plan_review` identity. Cover both step-slug identities and increment-scoped identities, preserve exact phase/identity/class, and do not put a Roadmap slug in `orphan_tasks` or relabel plan review as work review. Pin current-history fixtures for `structured-skeleton`, `checks-runner-worktree-name-collision`, `workflow-enforcement-tier-fold`, and the Q-81 increment population, with a phase-blind grouping red control.
2. R5-2: explicitly disposition non-Roadmap `work_review`. Either add a typed declaration arm or make future occurrences invalid while giving the eight retained records a bounded, typed historical treatment. Pin a record-379-shaped fixture and a red future record. Do not leave the shape accepted and unjoined.
3. R5-3: model convergence and single-pass processing as disjoint typed variants. The single-pass acceptance/review variant must have no constructible risk class, streak, foreclosure or cap, while retaining the high/critical dismissal re-check.
4. R5-4: supersede the real-parallelism gate at all three remaining requirements-scope sites, preserving write-path and authoritative-driving gates.
5. R5-5: replace acceptance record ordinals with the durable selector `task == "q78-design-pass" && phase == "acceptance"`, and keep those records outside the plan-review convergence window.

Update changed paths, documentation impact, architecture and Success Criteria consistently. Preserve Q-83, all statuses and converged identities. Do not implement code, edit pack, rewrite metrics history, close Q-58 or Q-78, or invent a decision.

Render and run both validations, strict render, tests, Clippy, diff and ASCII checks. Commit with a docs subject. Do not push.

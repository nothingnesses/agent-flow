# Brief: `Q-78` design pass, round 3 fix pass

This file is the brief itself. The orchestrator writes it before it dispatches, so a re-dispatch after a context loss reads this file rather than the orchestrator's memory.

## Your role

You are a PLANNER running an isolated fix pass. You author plan content. Your output goes through review, so round 4 judges what you write.

## The arithmetic that shapes this pass

The pass is `risky` and needs TWO CONSECUTIVE CLEAN ROUNDS. The streak is ZERO. THREE OF FIVE ROUNDS ARE USED. Convergence therefore needs rounds 4 AND 5 to both be clean, and a single valid finding in round 4 forecloses it.

Two consequences follow, and they bind you.

1. FIX WHAT IS ROUTED TO YOU AND AUTHOR NOTHING ELSE. Every new sentence you write is new unreviewed content that round 4 must judge. This is not the pass to improve something you notice in passing.
2. WHERE A FIX CAN DELETE RATHER THAN CORRECT, PREFER THE DELETION. This project has recorded repeatedly that a fix which deletes a stale figure beats a fix which corrects it, because the corrected figure expires again.

## Your inputs

- `docs/plans/agent-scaffold.reviews/q78-r7-triage.md` is AUTHORITATIVE. It carries a verdict per finding.
- `docs/plans/agent-scaffold.reviews/q78-r7-reviewer-groundblind.md` and `q78-r7-reviewer-adopter.md` are the raw findings.

THE TRIAGER'S VERDICT OVERRIDES THE REVIEWER'S SUPPORTING ARGUMENT. It upheld three findings while correcting the argument inside them, and in one case supplied a working wrong implementation because the reviewer's did not actually pass. Read the triage first and treat a reviewer's reasoning as superseded wherever the triage corrects it.

## What to do

1. FIX THE 18 UPHELD FINDINGS that are not the two accepted residuals. All 20 were upheld and none was dismissed.
2. RECORD THE TWO ACCEPTED RESIDUALS rather than fixing them. They are `A-F4`, `next` and `status` reporting success on a plan that does not parse, and `G-F9`, the numstat bound of 2 and 2 against a justification supporting 1 and 1. Each goes into its increment's residuals block, stating what a wrong implementation could then ship. The human accepted both under `AGENTS.md:57`.
3. DO NOT ACCEPT `G-F13`. It looks like the cheapest residual in the set and is not. Its remedy is one sentence, replacing a wrong pair of slugs with the measurement over the worklist, which the triage has already run and printed. Fix it.
4. FIX `A-F1`, `A-F3` AND `A-F6` AS CHANGED-PATH-SET AND DOCUMENTATION-IMPACT EDITS, NOT AS PACK EDITS. The human upheld the triager's ruling on this. The pass authors plan content only and must not touch `pack/`. So the fix records that the pack files must change, and adds them to the relevant increments' changed-path sets and WHAT CHANGES. The cost is accepted and recorded: the fix is a promise of an edit rather than the edit, and round 4 reviews the promise.
5. WRITE FOUR DECISION RECEIPTS to `docs/metrics/workflow.jsonl`, one per human decision of 2026-08-22, following the existing `type:"decision"` shape exactly. The four are recorded in the ledger as decisions (17) to (20). Use `q_id` values of the `Q-78-<suffix>` sub-decision form, since `provenance.decisions` resolves fail-closed to a registered `[[question]]` of `Q-<n>` shape and a sub-decision id parses to none. Each record's `options`, `recommendation` and `chosen` must match what the ledger records, and `chosen` must be the option the human actually took.
6. REGISTER `q78-design-pass` IN `[meta].orphan_tasks`, keeping the list's existing alphabetical order. Six round records now carry that task and it is not registered.

## What you must NOT do

- Do NOT touch `src/`, `Cargo.toml` or `pack/`. Verify at the end: `git diff main...HEAD -- src/ Cargo.toml pack/` must be empty.
- Do NOT flip `Q-78` to `decided`. The human has not decided it and a receipt would be false.
- Do NOT author a new step. `Q-80`'s codification step is owed and is NOT this pass's work, because `AGENTS.md` is generated from `pack/`.
- Do NOT edit this file, the findings files, or the triage file.
- Do NOT edit the ledger's `RESUME HERE` anchor or the paragraphs around it. The ledger is the orchestrator's. If a finding's remedy needs a ledger edit, say so in your final message instead of making it.

## The obligation on any criterion you re-author

Split each stated ground into its premise and its consequence, then build a wrong implementation for each half. The criteria must fail an implementation that falsifies the premise while the consequence still holds.

This is the third form of the obligation and round 3 is the first round run under it. Attacking a ground by checking its CONSEQUENCE passes whenever the consequence has another cause, which is why the second form was not enough.

## The three rules of the one clean sidecar

`ledger-order-citation-currency.md` has now had every stated figure reproduce exactly in two consecutive rounds. Apply its three rules to anything you write.

1. Its search set does not contain itself.
2. It refuses, as a stated rule, to write a concrete example into itself.
3. Every figure it states is a command's OUTPUT, never a pass condition.

Rule 3 is the one that broke a sibling sidecar, which wrote its expected counts as fixed numbers and then edited the very files those numbers count.

## The widened stop condition round 4 will be judged against

- CLASS 1, a ground-blind criterion: a wrong implementation passes while violating a stated risk ground, a numbered RULE or a cited Principle. COUNT ZERO.
- CLASS 2, now THREE kinds: a second-guard hole, a non-reproducing figure, and A CRITERION THAT REFUSES A CORRECT IMPLEMENTATION. Three or fewer, all `low` or `medium`.

The third kind is new this round. `A-F2`, `A-F6` and `G-F11` are that kind. When you repair a false refusal, check you have not replaced it with a ground-blind criterion.

## Gates

Run all six before you finish. The `<PLAN>` argument is required.

```
cargo test
cargo clippy --all-targets -- -D warnings
cargo run --quiet -- validate --source docs/plans/agent-scaffold.plan.toml --metrics docs/metrics/workflow.jsonl
cargo run --quiet -- validate --source docs/plans/agent-scaffold.plan.toml --workflow
cargo run --quiet -- render --check --strict docs/plans/agent-scaffold.plan.toml
LC_ALL=C grep -rcP '[^\t\x20-\x7e]' docs/plans/
```

`render --check --strict` will FAIL until you re-render, because you are changing the plan TOML and its sidecars. Re-render with the same command minus `--check --strict`.

## Known defects, so you do not re-discover them

- `render --check --strict` exits 2 when the `<PLAN>` argument is absent.
- `validate` exits 0 when its input file is absent, printing "nothing to validate". Never treat a bare exit 0 as proof. Pin the `N steps, M questions, valid` line.
- `grep -c` exits 1 when it matches nothing, and that is the PASS case for a sweep. Never chain a sweep with `&&`.
- This shell replaces `grep` with `ugrep`. Use `/usr/bin/grep` wherever an escape or a `-P` pattern matters.
- Never run `nix fmt` and never run `just scaffold-self`. The repository is not formatter-clean at `HEAD` and does not enforce it.

## File safety

Build every fixture ONLY under the session scratchpad, in a subdirectory you name yourself, and not in `groundblind/`, `adopter/` or `triage-r7/`, which other agents own. Do NOT write into bare `/tmp`. Do NOT delete anything outside your own fixture subdirectory. NEVER use a wildcard glob in a delete. Restore the mode of any 000 or 600 fixture before you finish.

## Your final message

State: how many findings you fixed, which two you recorded as residuals, the four receipt `q_id` values you wrote, the gate results, and any finding whose remedy you judged differently from the triage, with your reason. Name anything you were told to fix and did not.

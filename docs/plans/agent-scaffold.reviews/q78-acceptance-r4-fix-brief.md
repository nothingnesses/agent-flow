# Q-78 acceptance pass 4 repair brief

Act only as planner. Read AGENTS.md, both pass-4 reports, `q78-acceptance-r4-triage.md`, Q-83, the current plan schema constraints and affected driver/Q-58 records.

Repair all six findings without a new human decision:

1. R4-1: define a narrow typed compatibility rule that lets declared increment ids preserve existing uppercase historical identities such as `round-log-core-incA` and `round-log-core-incB`. Preserve exact joins. Do not rename ids, rewrite logs, add fake events, change statuses or use waivers. State the accepted token contract and red controls without weakening unrelated slug rules.
2. R4-2: add one typed plan-resident declaration for task-level convergence loops, keyed by task and convergence phase, with `risk_class` fixed before round one. It must cover the plan title and registered orphan tasks, feed task active-work and reconstruction, reject missing or mismatched snapshots, and carry Q-80 rebuild inheritance. Pin pre-round risky, risky-versus-low mismatch and live q78-design-pass shapes. Distinguish convergence-loop plan review from single-pass acceptance and standalone review.
3. R4-3: define a durable open-increment selection before `next` reads declarations. Reuse it in the active-work carrier and later fleet. Prefer an explicit increment identity on the step active-work arm or a strict declare-at-open lifecycle. Pin a converged low-risk sibling beside a newly selected risky sibling, and reject declaration-order/latest-round guesses.
4. R4-4: update both retained design records with a bounded Q-82 supersession note for the Stage-3 scheduler gate. Preserve unrelated write-path and authoritative-driver evidence gates.
5. R4-5: state that descriptive marginal/per-arm intervals and interval overlap are never decision rules, while a precommitted paired confidence bound may implement the frozen non-inferiority/equivalence rule. Apply it consistently to retained design, owning step and Success Criterion.
6. R4-6: update the authoritative Stage-2 paragraph to name `QuestionMachine` and the task/question/step fleet. Keep focused criteria as implementation detail.

Update dependencies, changed-path sets, documentation impact and Success Criteria. Preserve Q-83, Q-82 staging, all statuses and converged identities. Do not implement code or edit pack. Do not close Q-58 or Q-78.

Render and run both validations, strict render, tests, Clippy, diff and ASCII checks. Commit with a docs subject. Do not push.

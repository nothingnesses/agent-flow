# Q-78 acceptance pass 10, fresh-adopter closure lens (Claude)

Reviewed artifact: branch `review/q78-acceptance-r10-claude` at `7e635d8f`, whose product content is `plan/q78-design-pass` at `e6ddec1c` (the pass-9 repair) plus this pass's own brief commit. Baseline `main` at merge-base `8fd08150`. 108 changed files, all under `docs/`: `git diff --name-only main...HEAD | grep -v '^docs/'` returns nothing, so the product under acceptance is the planning artefact and not code. Lens: read the product first, then the ledger and the nine acceptance triages, and ask whether a fresh planner, implementer or Q-79/Q-86 explorer can execute what the plan now says from the durable artefacts alone.

## Result

ZERO SHORTFALLS. Every Success Criterion is met on the evidence below, the sole valid pass-9 disposition reproduces at both live sites in the exact form the triage prescribed, the two dismissed pass-9 claims are unchanged and remain correctly dismissed, every earlier disposition I sampled is repaired and still holding, and I found no doc or prompt this branch made stale.

## Verification actually performed

Toolchain built from this worktree: `cargo` and `rustc` 1.95.0 from `/nix/store/9l9lclxjw8ns5q4k13lxld7pl90paa3g-rust-mixed/bin` with `cc` from `/nix/store/adcz0m6qq2flmshdf0zz2xwjr5zbq1gr-gcc-wrapper-15.3.0/bin` (bare `cargo` fails with `linker 'cc' not found`). Every parser, checker, projection and mutation result below comes from HEAD's own binary (`agent-flow 0.0.4`), compiled in this worktree in this session, so nothing is adopted on trust. `CARGO_TARGET_DIR`, every scratch copy and every mutation stayed inside the authorised `TMPDIR` mount; no bare `/tmp` path was used and the live tree was never written. `git status --porcelain` was empty before and after this review, and `git worktree list` reports all four worktrees intact at the end (`agent-scaffold`, `q78-acceptance-r10-claude`, `q78-acceptance-r10-gpt`, `q78-planner`); nothing was pruned or removed.

Gates, all green:

- `cargo test`: exit 0. 470 passed, 0 failed, 0 ignored across 12 suites.
- `cargo clippy --all-targets -- -D warnings`: exit 0, zero warnings.
- `agent-flow validate --source docs/plans/agent-scaffold.plan.toml`: exit 0, `476 records, valid`, `114 steps, 87 questions, valid`.
- `agent-flow validate --source docs/plans/agent-scaffold.plan.toml --workflow --workflow-spec .agents/workflow.toml`: exit 0, `workflow invariants hold`.
- `agent-flow render --check --strict docs/plans/agent-scaffold.plan.toml`: exit 0, `up to date`.
- `agent-flow checks`: exit 0, `checks: 1 passed, 0 failed, 0 skipped` (the `render-check` lint row in `.agents/checks.toml`). Note for a later runner: the row invokes a bare `agent-flow`, so the check reports `sh: agent-flow: command not found` and fails unless the binary is on `PATH` in the isolated worktree. That is a runner-environment condition, not a product defect, and it is not branch-authored.
- `git diff --check` and `git diff --check main...HEAD`: exit 0, no whitespace or conflict-marker damage.
- ASCII: every one of the 108 branch-changed files is ASCII-clean. The only non-ASCII bytes anywhere in the tree are in 16 `docs/plans/agent-scaffold.reviews/*.md` findings files, and `git diff --quiet main...HEAD -- <each>` confirms all 16 are byte-identical to `main`, so none is branch-authored.

The gates were mutation-tested in scratch rather than trusted (Principle 11). In a scratch mirror of the plan sources, flipping the first `not-started` step to `complete` makes `validate --workflow` exit 1 with `Roadmap step 'test-driven' is 'complete' but has no round records and no covering waiver`; appending one line to the generated `docs/plans/agent-scaffold.md` makes `render --check --strict` exit 1 with `differs from a fresh render`; deleting `docs/plans/agent-scaffold.questions/Q-80.md` makes `render` exit 1 with `missing or unreadable sidecar`. The third mutation is a direct empirical confirmation of the existence contract the repaired sentences assert. The live tree stayed clean throughout.

## The sole valid pass-9 disposition reproduces at both live sites

The pass-9 triage ruled finding A valid at `low` and prescribed: at both live sites retain the existence-contract reasoning, replace the universal with a non-exhaustive form (question sidecars must exist and most are empty), make `find docs/plans/agent-scaffold.questions -type f -size +0` the sole current authority for the non-empty set, write no count and enumerate no path, and re-render.

Both sites now read as prescribed:

- `docs/plans/agent-scaffold.steps/plan-order-array-position.md:432`: "`render` treats a missing step or question sidecar as a hard failure, so question sidecars must exist and most are empty. Reproduce the current non-empty set with `find docs/plans/agent-scaffold.questions -type f -size +0`, and treat its path output rather than a count as the sole authority for that set."
- `docs/plans/umbrella-membership.explorations/Q-79.md:89`: the same non-exhaustive form and the same selector, with the wart restated as "Requiring otherwise-empty files only to satisfy that check", so the optional-context-sidecar argument still lands.

Neither site states a count and neither enumerates a path. The Step Detail source was re-rendered: the repaired sentence appears at `docs/plans/agent-scaffold.md:3415` and `render --check --strict` reports `up to date`. Q-79 is an exploration document and not spliced into the plan, so one rendered site is correct.

The selector is accurate as written. `find docs/plans/agent-scaffold.questions -type f -size +0` prints exactly `docs/plans/agent-scaffold.questions/Q-86.md` and `docs/plans/agent-scaffold.questions/Q-78.md`; `find docs/plans/agent-scaffold.questions -type f | wc -l` is 87, so "most are empty" is 85 of 87 and true.

I swept independently for any third live site carrying the retired universal. `rg -i "every question sidecar|all question sidecars|each question sidecar"` outside `agent-scaffold.reviews/` reaches only three places, all correctly qualified as historical rather than current: `docs/plans/step-intent-encoding.explorations/Q-78.md:219` ("the earlier claim that every question sidecar is empty is historical rather than an invariant"), `docs/plans/agent-scaffold.questions/Q-78.md:5` (which expressly supersedes the ask's observation), and `docs/plans/agent-scaffold.md:5061`, the render of that same sidecar. `rg "0 bytes|zero bytes|0-byte|zero-byte"` reaches three lines, of which the only product one is the historical Q-78 ask at `docs/plans/agent-scaffold.plan.toml:2404`. That ask is doubly marked: its own preamble at `:2382` says the paragraphs from "THE PASS RAN ON 2026-08-19" onward "SUPERSEDE every present-tense claim in the body below", and `:2404` sits inside that body; and `docs/plans/agent-scaffold.questions/Q-78.md:5` supersedes it a second time by name. The remaining hit is the ledger's dated round narrative at `:881`. No unqualified live universal survives.

## The two dismissed pass-9 claims are unchanged

- B, the five-step audit completeness claim. `docs/plans/agent-scaffold.steps/validate-missing-source-exit.md:25-27` still names the same five steps, still records the fifth step's accepted residual against receipt `q_id:"Q-78-residuals"`, and still records the separate `--workflow` protection. The pass-9 repair commit `e6ddec1c` touched only three files (`agent-scaffold.md`, `plan-order-array-position.md`, `Q-79.md`), so this source is untouched since the dismissal. Nothing about the enumerated five-step audit has changed, and the bounded historical explanation was correctly not converted into an open-ended branch inventory.
- C, the `CHANGELOG.md` Unreleased pointer. `CHANGELOG.md` still has no `Unreleased` heading and still opens at `## [0.0.4] - 2026-08-18`, and the pointer at `docs/plans/agent-scaffold.steps/workflow-calibration.md:3` still reads "(see the `## [Unreleased]` entry in `CHANGELOG.md`)". Diffing that clause against `git show main:docs/plans/agent-scaffold.steps/workflow-calibration.md` confirms the pointer is inherited verbatim from `main`: the branch reworded the surrounding deferral sentence but neither introduced nor changed the pointer. The dismissal stands on the same ground as in pass 9, and it must not be folded into Q-78 on this evidence.

## Earlier dispositions verified, with their fixtures re-measured

I re-derived the load-bearing numbers rather than adopting them.

- Pass-1 A1 (the waiver written into both substrates). `jq 'select(.type=="waiver")' docs/metrics/workflow.jsonl` now returns nothing, while `grep -c '^\[\[step.waiver\]\]' docs/plans/agent-scaffold.plan.toml` is 30. Criterion 36's canonical-source repair is scheduled by `docs/plans/agent-scaffold.steps/toml-primary-waiver-guidance.md`, and its two premises still reproduce: `pack/instrument.md:11` still presents `type: "waiver"` as the live exemption record, and `pack/plan-template.plan.toml:44` carries the commented `[[step.waiver]]` shape it points to.
- Pass-1 A5 (the self-contradicting decision inventory). The Q-78 ask now states no total and carries the selector instead; `grep '"ts":"2026-08-21' docs/metrics/workflow.jsonl | grep -o '"q_id":"Q-78[^"]*"' | sort -u` returns 16 ids, and no sentence in the ask block claims twelve or a six-plus-six split.
- Pass-1 T6 (the changed-path set foreclosing the changelog). `validate-missing-source-exit.md:120` now lists `CHANGELOG.md`, `src/main.rs` and `tests/validate_refuses_a_missing_explicit_path.rs`; `:126` schedules the `## [Unreleased]` entry; `:128` records the measured finding that `README.md` is not made stale, so criterion 39's "without inventing a README update" holds.
- Pass-5 R5-1 and pass-6 R6-G1 (the phase/owner shapes the two-arm model could not represent). The current-history fixture in criterion 37 reproduces exactly. All 99 `plan_review` records partition once: 7 exact-step records over 2 identities (`structured-skeleton` 3, `checks-runner-worktree-name-collision` 4), 15 records over the 3 fieldless-increment identities (`decision-folder-currency-fold` 5, `workflow-enforcement-tier-fold` 5, `workflow-enforcement-tier-endproperty-fold` 5), and 41 records over the 13 Q-81 increment identities, giving 63 Roadmap-resolving records over 18 identities; the other 36 resolve to 14 non-Roadmap task identities, every one of which is registered in `[meta].orphan_tasks`. `decision-folder-currency-fold` carries its own `low_risk` increment declaration at `docs/plans/agent-scaffold.plan.toml:1267-1269`, and all 13 Q-81 delivery increments are declared with `risk_class = "risky"`. The plan's own count of unowned work review also reproduces: exactly 8 `work_review` rounds resolve to no Roadmap step, at records 1, 47, 48, 164, 173, 177, 178 and 379, because the 9 further candidates a naive lexical strip flags (158, 160-162, 166-170) each carry structured `step`/`increment` ids.
- Criterion 38's bounded windows reproduce. The 7 `q78-design-pass` `plan_review` records occupy records 380-386 inclusive, exactly "through record 386", and all 9 records selected by `task == "q78-design-pass" && phase == "acceptance"` (432-437, 473, 475, 476) fall outside that window. Records 387-430 hold exactly 41 `plan_review` rounds over 13 distinct increment identities plus 3 escalation records, matching "its exact 41 rounds and three escalation segments".
- Pass-8's two repairs hold. `workflow-calibration.md` no longer states an exhaustive "the directory currently holds" inventory, and the Q-78 exploration's sidecar assertion at `:219` is now marked historical.

The metrics log remains append-only against the baseline. `main`'s 438 records are a byte-exact prefix of HEAD's 476; `git diff --numstat main...HEAD -- docs/metrics/workflow.jsonl` is `38 0`, and the 38 appended records are 34 `decision`, 3 `round` and 1 `intake`. No line was rewritten or removed.

## Other measured claims I re-ran rather than trusted

- `docs/plans/agent-scaffold.steps/ledger-order-citation-currency.md:29-37`. Running the four-date script verbatim (with its temporary file redirected into authorised scratch) prints `then=8 agreeing=8`, `then=11 agreeing=11`, `then=12 agreeing=12` and `then=18 agreeing=18`, exactly as stated. Its companion claim also holds: `order` values 84 and 91 are the only two vacant in 1..116, and the ledger cites both.
- `docs/plans/agent-scaffold.steps/plan-order-array-position.md:9`, "the two agree today at every step except one". Comparing declaration sequence against the `order` field, the sequence has exactly one descent, at `rename-to-agent-flow` (order 100 at array position 84); removing that single step makes the sequence strictly monotone.
- `docs/plans/umbrella-membership.explorations/Q-79.md:60-71`. Both selectors reproduce: the first prints the same 7 slugs it names, and the second prints exactly 10 rows, being those 7 plus `convergence-accounting`, `file-safety-rules` and `exploration-mode`. The stated `core-assets` exception is also true: its first heading line is `### \`core-assets\`:` at line 9 while its first line is prose.
- `docs/plans/agent-scaffold.steps/step-intent-encoding.md:32`, the "210 on the tree this sidecar was spliced into" figure. It is correctly dated rather than stale: the sentence was introduced at `f5e3902c`, where `grep -c '^\[\[step\]\]'` is 105, so `2 x 105` is 210, and the sentence carries the live command as the current authority.
- Criterion 35's byte datum. `docs/plans/code-value-audit.explorations/Q-58-ablation-design.md:17-28` pins the reopening measurement to commit `3ad7b2b` (an ancestor of `main`) at 1434 lines, 113409 words and 727754 bytes, and labels it historical rather than a pass condition. Every other reference in the tree says only "roughly 728 KB" or points here, as that section claims. My own fresh run of `next` on this branch produced 1438 lines and 734194 bytes, which is consistent with the stated growth caveat and confirms the datum was never written as a current target.
- Criterion 41. `docs/plans/agent-scaffold.steps/workflow-calibration.md:18` pins the Q-85 receipt at `docs/metrics/workflow.jsonl:472`, and line 472 is that receipt; its documented `jq` selector returns the three presented options with `recommendation` and `chosen` both `Add a design pass to workflow-calibration`. Q-86 is `exploring` at `docs/plans/agent-scaffold.plan.toml:2553-2555`, and the tracked brief `docs/plans/workflow-calibration.explorations/Q-86-convergence-mechanism-brief.md` exists.
- Criterion 35's gating. `q58-output-ablation` blocks `resume-state-currency-signal`, which in turn blocks `workflow-driver-typed-fleet`, which blocks `workflow-ready-frontier-scheduler`, so the typed fleet is transitively blocked until the human decides the carrier. Q-58 is `exploring`.
- Citation spot-checks in branch-authored prose all resolve exactly: `src/plan/render.rs:135` is `render_plan`, `:180` is the `format!("{}.md", step.slug)` filename binding, and `:573` is `step_details_section` whose doc comment carries the quoted "each sidecar carries its own `### <slug>` heading". Across all 849 backticked path citations in the branch product, no citation with a line number points past the end of its file.

I also ran `agent-flow next --source docs/plans/agent-scaffold.plan.toml` (exit 0). It selects `workflow-calibration` and reports `awaiting-first-review` for a step that is in a Q-86 exploration, which is precisely the Q-82 defect `docs/plans/agent-scaffold.steps/workflow-loop-visibility.md:5` describes and schedules. That is the plan correctly documenting a live defect it owns, not a contradiction.

## Documentation currency

I found nothing this branch made stale. The branch changes only `docs/`; it touches no `pack/` file, no prompt, no `README.md`, no `AGENTS.md` and no `src/`, so it cannot have invalidated a shipped doc by changing behaviour. The generated view is in sync (`render --check --strict`), and the plan's front and tail matter (`_status-narrative`, `repo-layout`, `roadmap-intro`, `queue-intro`, `documentation-protocol`, `motivations`, `principles-note`) states no step, question or criterion count that the branch's additions could have expired.

The one stale-looking pair I did find is inherited and out of scope on the ground pass 9 settled for claim C. `docs/plans/agent-scaffold.repo-layout.md:13-14` still says `src/` holds "four modules" and that the pack ships `plan-template.md`, while the pack now ships `plan-template.plan.toml` and the crate has more modules. `git diff --quiet main...HEAD -- docs/plans/agent-scaffold.repo-layout.md` confirms the file is byte-identical to `main`, and the pack migration that outdated it landed in earlier completed steps already on `main`. The acceptance documentation-currency rule reaches docs made stale by the shipped change; counting this one would expand Q-78 to cover every pre-existing stale reference an accurate new reading happens to expose. It is not an accepted residual either, and a separately scoped documentation-maintenance task is the right home for it.

# Q-78 acceptance pass 8 repair brief

Act only as planner in this isolated worktree. Read AGENTS.md, both pass-8 reports, `q78-acceptance-r8-triage.md`, the complete product and the ledger. Repair exactly the two valid low shortfalls. Do not act on Claude A, which triage dismissed.

1. In `docs/plans/step-intent-encoding.explorations/Q-78.md`, remove the exhaustive claim that the other question sidecars remain empty. Retain the existing `find` command and make its path output the sole authority for the current non-empty set. Do not replace the stale one-file inventory with a two-file inventory.

2. In `docs/plans/agent-scaffold.steps/workflow-calibration.md`, replace the exhaustive `The directory currently holds` three-file claim with a non-exhaustive description. The three named records may remain identified as core audit records. Refer to the directory as also carrying related calibration and design records. Do not state a file count or enumerate all current files. Do not add `2026-08-14-causation-investigation.md` as a mandatory Q-86 input because triage rejected that consequence.

Re-render the generated plan. Do not change questions, receipts, Success Criteria, Roadmap statuses, workflow rules, code, packs, the Q-86 evidence sample or any other finding disposition.

Run both validation modes, strict render, tests, Clippy, diff checks and ASCII checks. Do not run `agent-flow checks` inside the container because its host worktree view can be incomplete. The orchestrator will run it on the host. Commit with a docs subject. Do not push.

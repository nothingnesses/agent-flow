# Q-78 acceptance pass 9, fresh-adopter closure lens (Claude)

Reviewed artifact: branch `review/q78-acceptance-r9-claude` at `52bbef19`, whose product content is `plan/q78-design-pass` at `12bae0ac` (the pass-8 planner repair) plus this pass's own brief commit. Baseline `main` at merge-base `cef35355`. 107 changed files, all under `docs/` (`git diff --name-only main HEAD | grep -v '^docs/'` returns nothing), so the product under acceptance is the planning artefact and not code. Lens: read the product first, then the ledger and the eight acceptance triages, and ask whether a fresh planner, implementer, or Q-79/Q-86 explorer can execute what the plan now says from durable artefacts alone.

## Verification actually performed

Toolchain built from this worktree: `cargo` and `rustc` 1.95.0 from `/nix/store/9l9lclxjw8ns5q4k13lxld7pl90paa3g-rust-mixed/bin`, with `cc` from `/nix/store/adcz0m6qq2flmshdf0zz2xwjr5zbq1gr-gcc-wrapper-15.3.0/bin` (bare `cargo` fails with `linker 'cc' not found`). Every parser, checker and projection result below comes from HEAD's own binary (`agent-flow 0.0.4`), compiled in this worktree in the same session, so nothing is adopted on trust. Build output went to the gitignored `/target/`; `git status --porcelain` is empty at the end of this review. Every copy, scaffold, mutation and probe stayed inside the authorised `TMPDIR` scratch mount and no bare `/tmp` path was used. `git worktree list` reports all four worktrees intact at the end (`agent-scaffold`, `q78-acceptance-r9-claude`, `q78-acceptance-r9-gpt`, `q78-planner`); nothing was pruned or removed.

- `cargo test`: exit 0. 470 passed, 0 failed, 0 ignored across 12 suites.
- `cargo clippy --all-targets -- -D warnings`: exit 0, zero warnings.
- `agent-flow validate --source docs/plans/agent-scaffold.plan.toml`: exit 0, `475 records, valid`, `114 steps, 87 questions, valid`.
- `agent-flow validate --source docs/plans/agent-scaffold.plan.toml --workflow --workflow-spec .agents/workflow.toml`: exit 0, `workflow invariants hold`.
- `agent-flow render --check --strict docs/plans/agent-scaffold.plan.toml`: exit 0, `up to date`.
- `agent-flow checks` through a scratch-only `PATH` shim: `1 passed, 0 failed, 0 skipped`, exit 0.
- `git diff --check` and `git diff --check main HEAD`: exit 0.
- ASCII: `LC_ALL=C grep -cP '[^\t\x20-\x7e]'` sums to 0 over all 107 files changed since the merge-base.

FRESH-SCAFFOLD CRITERIA HOLD. `scaffold --output-dir <scratch> --write --principles default --vcs none` writes 31 files; the dropped `docs/plans/TEMPLATE.plan.toml` validates (`1 steps, 0 questions, valid`) and `render --check --strict`es `up to date`; the only `w4_baseline` occurrence is the explanatory comment at `:10`, so no baseline is declared; and a default re-run refreshes 18 reference assets while leaving all 13 working-ownership assets untouched, including a marker line appended to `AGENTS.md`.

## The two pass-8 dispositions and the dismissed sample

### R8-G1 (the Q-78 exploration's stale question-sidecar assertion): CLOSED, and the selector is now the sole authority

`docs/plans/step-intent-encoding.explorations/Q-78.md:44` now reads "Question sidecars were all empty when this pass first measured them. Decision 26 now lives in `docs/plans/agent-scaffold.questions/Q-78.md`. Reproduce the current non-empty set with `find docs/plans/agent-scaffold.questions -type f -size +0`, and treat its path output rather than a count as the sole authority for that set." The exhaustive "while the rest remain empty" clause is gone and no two-file inventory replaced it, which is exactly the triage's smallest durable disposition. Running the retained selector prints `docs/plans/agent-scaffold.questions/Q-78.md` and `docs/plans/agent-scaffold.questions/Q-86.md`.

### R8-C2 (`workflow-calibration`'s exhaustive directory inventory): CLOSED, and de-counted rather than re-enumerated

`docs/plans/agent-scaffold.steps/workflow-calibration.md:9`, rendered at `docs/plans/agent-scaffold.md:708`, now reads "The audit's core records there are ... The directory also carries related calibration and design records." No file count is stated and no complete list is asserted, while the three descriptions the triage said to retain are retained verbatim. `git ls-files docs/plans/workflow-calibration.explorations/` prints thirteen paths and nothing in the sentence contradicts that. The triage's two explicit prohibitions are both honoured: no new mandatory Q-86 reading obligation was added, and `2026-08-14-causation-investigation.md` was not inserted into the brief's reading list.

### The dismissed fixed Q-86 evidence sample: UNCHANGED

`git diff 8d8a24a8 HEAD -- docs/plans/workflow-calibration.explorations/Q-86-convergence-mechanism-brief.md` is empty, so the repair commit `12bae0ac` did not touch it. `:27` still frames "The six recorded Q-78 acceptance passes", `:33` still reads "At this fold the selector returns six passes and `[7,10,5,6,5,5]`" and still enumerates `q78-acceptance-triage.md` and `q78-acceptance-r2-triage.md` through `q78-acceptance-r6-triage.md`. The bounded historical sample the pass-8 triage ruled correct is intact. I do not re-raise it: the selector at `:30` now returns eight passes and `[7,10,5,6,5,5,4,2]` (records 432-437, 473 and 475), which is the moving current figure the brief's own instruction at `:27` tells an explorer to derive, and the triage settled that the fixed six-pass sample is not an inventory claim.

## The four pass-7 dispositions: all four still CLOSED under independent measurement

- R7-1 (the non-admissible baseline). `Q-86-convergence-mechanism-brief.md:23` still reads "a required comparison baseline but not an admissible bounded candidate ... The baseline is not recommendation-eligible", and the five downstream obligations stay re-scoped consistently at `:87`, `:89`, `:91`, `:93` and `:97`. The same boundary is carried in the `[[question]]` Q-86 ask, in `docs/plans/agent-scaffold.questions/Q-86.md`, and in `workflow-calibration.md:20`. Q-86 is still `exploring` with no `folded_into` and no receipt.
- R7-2 (the newly authored status label). `workflow-calibration.md:3` still opens token-free with "The scaffolded workflow's round constants and the second-triager guard were introduced after a design review", and neither the anchored nor the relaxed drift selector reaches `workflow-calibration` at HEAD.
- R7-3 / Q-87 (the drift measurement). Running criterion 1's anchored selector (`sidecar-status-opening-drift.md:126-133`), criterion 1's relaxed selector (`:139-145`) and the H1 script transcribed verbatim from `:172-187` against HEAD prints `relaxed=43 anchored=34 strip-relaxed=26 strip-anchored=17`, exactly what `:21-24` and the Q-87 decision paragraph state. The nine-row relaxed-minus-anchored difference is exactly the nine adjectival `Deferred <noun>` openings `:26` enumerates. The 17-member handover is `include-all-visible state-schema workflow-viz optional-modules greenfield-flake later-enhancements git-url-fetch tui-authoring instrument-flag escalation-exempt waiver-model structured-skeleton test-driven mutation task-entry-regrounding reviewer-reproducible-evidence workflow-audit-followups`. Q-84's 44/35/27/18 is preserved as dated history at `agent-scaffold.plan.toml:2537` and `sidecar-status-opening-drift.md:28`, and nothing states it as current.
- R7-4 (Q-85 provenance and exact receipt). The `[[step]]` block carries `[step.provenance] decisions = ["Q-85"]` (`agent-scaffold.plan.toml:483-484`), and `workflow-calibration.md:18` names `docs/metrics/workflow.jsonl:472` plus a commit-independent `jq` selector. `sed -n '472p'` is the Q-85 decision object with `chosen` a member of `options`, and the quoted selector returns exactly one line. The ordinal is still stable: records 473, 474 and 475 sit after it.

## Criteria I re-derived rather than read

- CRITERION 35's FIXTURE PARTITION REPRODUCES EXACTLY, using the tool's own join (a round's step is its structured `step` id else `task` with a trailing `-inc<alnum>` stripped; its increment is its structured `increment` id else the full `task`). 99 `plan_review` records; 63 Roadmap-resolving over exactly 18 identities; 36 non-Roadmap over exactly 14 task identities. The 63 partition into seven exact-step records (`checks-runner-worktree-name-collision` 4, `structured-skeleton` 3), 41 Q-81 historical increment records, and 15 on the three live plan-review increment declarations (`decision-folder-currency-fold` 5, `workflow-enforcement-tier-fold` 5, `workflow-enforcement-tier-endproperty-fold` 5). The observed classes match the declarations the spec promises: `decision-folder-currency-fold` and both new orphan registrations `q77-fold`/`step-intent-fold` snapshot `low_risk`, and `q78-design-pass` snapshots `risky`.
- THE Q-81 ORDINAL MAP AT `structured-risk-class-source.md:28` REPRODUCES ROW FOR ROW. Records 387 to 430 hold exactly 41 rounds over the thirteen listed ids with the listed ordinals, plus the three escalations at 421, 422 and 429 and nothing else; all 41 are `plan_review` and all 41 snapshot `risky`.
- ALL FIFTEEN DIGEST PINS REPRODUCE. `sed -n "${n}p" docs/metrics/workflow.jsonl | tr -d '\n' | sha256sum` matches `:34` for records 98, 99, 115, 217, 219, 220 and 221 and `:38` for records 1, 47, 48, 164, 173, 177, 178 and 379. The cap-adoption digest at `review-loop-foreclosure-enforcement.md:21` also matches record 386 (`4e7aed64e77ca228347de1cfbcf620ca2d6b6f3d8550b606212b3b448d0e95c6`).
- CRITERION 36's BOUNDED SEGMENT STILL HOLDS UNDER TWO APPENDED ACCEPTANCE ORDINALS. `task == "q78-design-pass" && phase == "plan_review"` selects exactly records 380 to 386 and no escalation record exists for that task; `phase == "acceptance"` now selects eight records (432 to 437, 473 and 475), every one outside the `through_record = 386` window. Records 383 and 384 do describe an escalation and a rebuild in their `artifact` prose while carrying `type:"round"`, which is the exact fact `:19` records as unable to segment reconstruction.
- CRITERION 33 REPRODUCES AGAINST ITS DESIGN. Section 4 of `Q-58-ablation-design.md` enumerates every frozen field `q58-output-ablation.md:7` says the pre-run protocol requires (margin, confidence level, interval/test method, state aggregation and weights, cross-model aggregation, disagreement rule, tie-break, retained-aggregate schema and analysis command), and both the design and the owning step state the same descriptive-interval-versus-paired-decision rule. The plan graph enforces the criterion's ordering: `resume-state-currency-signal` is `blocked_by = ["q58-output-ablation"]` and `workflow-driver-typed-fleet` is blocked by that plus `workflow-loop-visibility` and `review-loop-foreclosure-enforcement`.
- CRITERION 37's SOURCE GRAMMAR IS EXECUTABLE AS WRITTEN. The R2 arm in `step-intent-encoding.md` admits only `docs/plans/agent-scaffold.md` and `docs/plans/agent-scaffold.steps/<slug>.md` for a prose source and reports anything else as `SOURCE NOT THIS STEP`; the bare metrics path is rejected with `RECEIPT LINE REQUIRED`; the receipt arm requires a schema-valid `type:"decision"` object whose `task` equals the row's slug and exposes only `chosen`/`recommendation`/`options`. The paragraph-valued recommendation, the four-item residual pointer and the two receipt selectors agree across the `[[question]]` ask, `docs/plans/agent-scaffold.questions/Q-78.md`, `step-intent-encoding.explorations/Q-78.md:7,82-83,215,301,324` and `step-intent-encoding.md:1073`. Residual 4's measured claim reproduces exactly on today's binary: against a scratch plan omitting the already-required `title`, `validate --source` exits 1 with the parse error while `next --source` prints `no active review loop (no plan steps found)` and `status --source` prints `plan: not provided`, both at exit 0.
- CRITERION 39 HOLDS IN FULL. `workflow-calibration` is `in-progress`, records Q-85's decision and its exact receipt, keeps Q-86 `exploring`, schedules the tracked brief, and `:20` plus `Q-86-convergence-mechanism-brief.md:7` both state that no cap, acceptance, reset, pack or code behaviour changes before the later human decision.
- `workflow-driver.md`'s REWRITTEN OPENING IS TRUE ON THE LOG. Its `[[step]]` is `in-progress` and declares `workflow-driver-stage0a`, `-stage0b` and `-stage1`, all `risky`; the nine joined rounds are all `work_review`, all snapshot `risky`, and each of the three increments reaches `consecutive_clean = 2`.
- Q-79's POSITIONAL EVIDENCE REPRODUCES ROW FOR ROW. Its own command prints `convergence-accounting` at 13 with members at 13, 14, 15, 16, 20, 23, 24, 27 and non-members at 17, 18, 19, 21, 22, 25, 26; `file-safety-rules` at 17 with members 17, 18, 19, 22 split by 21; `findings-files` at 26, nine rows below; and `exploration-mode` at 37 with members 37, 40, 41, 46 and six non-members between them. The five umbrella titles each render exactly once.
- DUTY (h) IS REPRODUCED AND UNCHANGED. `next --source docs/plans/agent-scaffold.plan.toml` reports `workflow-calibration` as the active loop at `awaiting-first-review`, `rounds 0/5`, offers a reviewer, and emits 733759 bytes over 1438 lines. Both are the limits Q-82 and the reopened Q-58 schedule; criterion 33 records the byte totals as evidence and never as a target.
- EVERY ROOTED `<path>:<line>` CITATION IN THE BRANCH'S CHANGED PLAN SOURCES RESOLVES. Over the 43 changed non-findings Markdown/TOML files, 473 rooted path citations name an existing file with every cited line in range; the remainder are bare filenames resolved by their directory context, deleted findings files recoverable from history, and forward references to files a not-started step will create (`Q-58-ablation-protocol.md`, `Q-58-ablation-result.md`, `src/driver/*.rs`, `tests/validate_refuses_a_missing_explicit_path.rs`).

## Shortfalls

### A. `low` - the false universal "every question sidecar is 0 bytes" survives at two live sites, one of them contradicting its own sibling bullet inside the generated plan

The pass-8 repair corrected this claim in `step-intent-encoding.explorations/Q-78.md:44`. The identical universal is still live in two places the repair did not reach:

- `docs/plans/agent-scaffold.steps/plan-order-array-position.md:432`, rendered verbatim at `docs/plans/agent-scaffold.md:3415`: "THE SIDECAR EXISTENCE CONTRACT. `render` treats a missing step or question sidecar as a hard failure, which is why every question sidecar is 0 bytes."
- `docs/plans/umbrella-membership.explorations/Q-79.md:89`: "Every question sidecar is 0 bytes and exists only to satisfy that check, which is a wart in the reader's contract."

MEASURED AT HEAD, `find docs/plans/agent-scaffold.questions -type f -size +0` prints two paths of 87: `Q-78.md` (1295 bytes) and `Q-86.md` (510 bytes). Both universals are false.

THIS IS A CONCRETE INTERNAL CONTRADICTION INSIDE ONE GENERATED DOCUMENT, WHICH IS WHAT LIFTS IT ABOVE A NIT. `docs/plans/agent-scaffold.md:3415` and `:4689` are the parallel NOT-IN-SCOPE bullets of the two blocking-related steps of the same design pass, and they now disagree with each other. `:4689` (from `step-intent-encoding.md:1093`) carries the corrected de-counted form: "THE QUESTION-SIDECAR EXISTENCE CONTRACT. Most remain empty and exist to satisfy `render`'s existence check; `Q-78.md` now carries decision 26 as supplemental prose." `:3415` still asserts the universal the other bullet retired. A third site, `docs/plans/agent-scaffold.md:5061` (the rendered `Q-78.md` question sidecar), states outright that "This non-empty body also supersedes the question ask's historical observation that every question sidecar is empty", so the same generated file both records the supersession and republishes the superseded claim.

THE BRANCH OWNS BOTH HALVES, WHICH IS WHY THIS IS A PASS-9 SHORTFALL RATHER THAN INHERITED NOISE. `git log -S` puts both stale sentences at `30695a34`, and neither line exists on `main`. `63bf1316` is the commit that made `Q-78.md` non-empty and, in the same commit, corrected the parallel statements at `step-intent-encoding.explorations/Q-78.md:219` and `step-intent-encoding.md:815`; `41b8f07b`/`edccdb2e` then made `Q-86.md` non-empty; and `12bae0ac` corrected a fourth site. So this branch has corrected four instances of one claim and left two, one of which ships into the published plan.

THE FRESH-EXPLORER CONSEQUENCE IS ON `Q-79.md:89` SPECIFICALLY. Q-79 is `exploring` and that exploration is its durable handoff. The sentence is load-bearing prose in the argument WHY THE CONTEXT SIDECAR IS OPTIONAL, cited as the corpus evidence that the existence contract is a wart. A later explorer or planner reading it as current will state a premise the tree falsifies with one `find`, in a document whose own preamble promises that "every figure below is measured ... and every figure carries the command that reproduces it". This sentence carries no command.

WHY IT IS `low`. Neither operative conclusion depends on the universal: `render` genuinely does fail on a missing sidecar (`render_plan`, `src/plan/render.rs:135`), and Q-79's optional-sidecar argument survives on its independent clause about a scaffolded project having to create an empty file. No Success Criterion states a sidecar count, and the correct fact is one command away.

SMALLEST SAFE DISPOSITION. Give both sites the de-counted form `step-intent-encoding.md:1093` already uses, so the pass has one wording rather than two: say that most question sidecars are empty and exist to satisfy `render`'s existence check, and let `find docs/plans/agent-scaffold.questions -type f -size +0` be the authority for the current non-empty set, exactly as the pass-8 disposition directed for the exploration. Do not enumerate the two non-empty files, and do not write a count. Re-render after the `plan-order-array-position.md` change.

### B. `low` - `validate-missing-source-exit` asserts its bare-exit-0 audit covers "ALL FIVE OF THE PASS'S STEPS" while the pass now owns fourteen and nine carry the un-audited pattern

`docs/plans/agent-scaffold.steps/validate-missing-source-exit.md:25` reads: "THIS STEP BLOCKS NOTHING IN THE `Q-78` PASS, AND THE ENUMERATION BEHIND THAT CLAIM COVERS ALL FIVE OF THE PASS'S STEPS", then names `plan-order-array-position`, `step-intent-encoding` and `ledger-order-citation-currency` as pinning the stdout summary line, itself as the fourth, and `sidecar-status-opening-drift` as the fifth whose criterion 8 reads a bare exit 0. The paragraph closes by self-correcting an earlier form for concluding about five while measuring three, and names that generalisation as "the pass's own named failure mode".

MEASURED AT HEAD, THE PASS'S STEP SET IS NOT FIVE. `docs/plans/agent-scaffold.plan.toml:2433` records that the acceptance repairs authored `sidecar-status-authored-openings` and gave separate Roadmap ownership to `structured-risk-class-source`, `review-loop-class-inheritance`, `review-loop-foreclosure-enforcement` and `toml-primary-waiver-guidance`, and the Q-82 fold added `q58-output-ablation`, `workflow-loop-visibility`, `workflow-driver-typed-fleet` and `workflow-ready-frontier-scheduler`. All nine sidecars are new on this branch. All nine carry a closing acceptance criterion of the form "Both validation modes, strict render, tests, Clippy, diff checks and ASCII checks pass" and none of the nine mentions the `<N> steps, <M> questions, valid` stdout line:

```
grep -l 'Both validation modes' docs/plans/agent-scaffold.steps/*.md            # 9 files
grep -c 'questions, valid' docs/plans/agent-scaffold.steps/<each of the nine>.md # 0 for all nine
```

THE MISS IS LOCALISED TO ONE COMMIT. `a8ea21fb` ("docs: repair Q-78 acceptance plan and schedule driver work") added eight of the nine sidecars AND edited `validate-missing-source-exit.md` in the same commit, appending its DOCUMENTATION IMPACT section while leaving the five-step enumeration untouched. `git log -S 'COVERS ALL FIVE OF THE PASS'` shows the phrase has stood unchanged since `19165328`.

WHAT IS AND IS NOT AT RISK, MEASURED. The safety half of the claim survives: "Both validation modes" includes the `--workflow` arm, and MEASURED in an empty directory outside the repository `validate --source docs/plans/TEMPLATE.plan.toml --workflow` exits 1 with `--workflow requested but no plan source resolved`, so none of the nine criteria can be satisfied on a tree with no plan. That is the same reasoning `sidecar-status-opening-drift.md:250` records for its own accepted residual. What is false is the COMPLETENESS claim: a fresh implementer or a later reviewer reading `:25` is told the pattern has been swept from the pass, and it has not been swept from nine of its fourteen steps.

WHY IT IS `low`. Nothing wrong can ship, the residual for the one enumerated case is already human-accepted (`q_id:"Q-78-residuals"`), and the fix is a wording change rather than a criteria rewrite.

SMALLEST SAFE DISPOSITION. Replace the fixed "ALL FIVE" scope with the same selector-over-snapshot form the rest of this pass now uses: state which steps pin the stdout line, state that the remainder rely on the `--workflow` arm for the same protection with the reason given once, and give the command that reproduces the current set (`grep -l 'Both validation modes' docs/plans/agent-scaffold.steps/*.md`). Do not add a stdout pin to the nine successors, and do not write a step count into the paragraph.

### C. `low` - the plan tells a reader to see a `## [Unreleased]` CHANGELOG entry that the same plan measures as absent

`docs/plans/agent-scaffold.steps/workflow-calibration.md:3`, rendered at `docs/plans/agent-scaffold.md:702`, is the only durable statement of where the round constants came from: "The scaffolded workflow's round constants and the second-triager guard were introduced after a design review (see the `## [Unreleased]` entry in `CHANGELOG.md`)".

MEASURED AT HEAD, `grep -n 'Unreleased' CHANGELOG.md` prints nothing and exits 1; `grep -n '^## ' CHANGELOG.md` prints `## [0.0.4] - 2026-08-18`, `## [0.0.3]`, `## [0.0.2]` and `## [0.0.1]`. The section was closed into `## [0.0.4]` by the release commit `dcaf1856`, so the pointer resolves to nothing.

THE CONTRADICTION IS INTERNAL TO THE GENERATED PLAN AND THIS BRANCH AUTHORED THE DISPROVING HALF TWICE. `docs/plans/agent-scaffold.md:3400` (from `plan-order-array-position.md:417`) and `:4661` (from `step-intent-encoding.md:1065`) both read "`grep -n 'Unreleased' CHANGELOG.md` exits 1 today, so the entry means opening the section rather than appending to one". Both sentences are new on this branch and both are correct. So one generated document tells a reader at `:702` to consult a section that it proves absent at `:3400` and `:4661`.

PROVENANCE, STATED PLAINLY SO THE TRIAGE CAN WEIGH IT. The false half is INHERITED: `git show main:docs/plans/agent-scaffold.steps/workflow-calibration.md` carries the identical parenthetical, which dates to `0fadd90f` and went stale at the 0.0.4 release, both on `main`. What is new is that this branch put the measurement that disproves it into the same rendered file, and that the pass-7 repair edited that exact line (deleting the leading `In progress. ` token) under a disposition that explicitly forbade widening the edit. `workflow-calibration` is also the step criterion 39 names, so this is the one inherited staleness sitting inside an acceptance-named surface.

WHY IT IS `low`. The sentence states the three constant values (1, 2 and 5) itself, so the citation is corroborative rather than load-bearing, and the reader loses one failed lookup rather than a fact. It is a shortfall rather than a nit because AGENTS.md phase 5 makes doc currency an acceptance duty and the plan now carries its own disproof.

SMALLEST SAFE DISPOSITION. Point the parenthetical at the section that actually holds the record, or drop the parenthetical and keep the sentence, which stands on its own. Do not reopen the 0.0.4 section, and do not touch `plan-order-array-position.md:417` or `step-intent-encoding.md:1065`, which are correct as measured. Re-render after the sidecar change.

## Recorded but not filed

- **The Q-86 brief's six-pass sample is now eight.** `:33` still states six and `[7,10,5,6,5,5]`; the selector at `:30` returns eight and `[7,10,5,6,5,5,4,2]`. Not filed: the pass-8 triage adjudicated exactly this and ruled it a bounded historical sample rather than a live inventory, the brief's own `:27` directs an explorer to derive the current figures, and I have no evidence that verdict was wrong. This pass's instruction was to confirm the sample is unchanged, and it is.
- **The ledger's resume anchor still reads "Q-78 ACCEPTANCE PASS 7 TRIAGED, HUMAN DECISION REQUIRED"** (`docs/plans/agent-scaffold.ledger.md:535`) while the same block records passes 7 and 8 complete and repaired and names pass 9 as the immediate next action. The heading has stood unchanged since `fc22cef5`, through two pass records. Not filed: the ledger is the orchestrator's transient artefact, deleted at task close; the operative next action is stated correctly in the same block; and no Success Criterion covers the ledger heading.
- **Ledger obligation items (3) and (4) remain unregistered and item (5) still reads falsely** (`Q-80` is `decided -> folded into review-loop-class-inheritance`, not `open`). Not filed, on the same ground the last two passes recorded: the paragraph heads itself "HISTORICAL PRE-ACCEPTANCE OBLIGATION LIST, RETAINED AS EVIDENCE AND NOT AS CURRENT INSTRUCTION".
- **`sidecar-status-authored-openings.md:5` names Q-84's 18 and not Q-87's 17.** Not filed: everything it says is true, and its operative rule ("no implementation criterion hard-codes either dated count") is exactly what Q-87 confirms. Pass 8 recorded the same and I have no new evidence.
- **The exploration's `45`/`21` status-opening figures remain superseded** (`step-intent-encoding.explorations/Q-78.md:45`) against a relaxed selector that now reaches 43. Not filed: the exploration's preamble states that figure grows, `sidecar-status-opening-drift` carries the selecting command, and `step-intent-encoding.md` rule 9 retires the 45 explicitly.
- **`validation-constraints.md:171` describes a `## [Unreleased]` section "which has `Added` and `Changed` and no `Fixed`".** Same absent-section class as finding C, but that step is untouched by this branch and identical on `main`, with no new measurement placed beside it, so it is inherited rather than this product's.
- **`step-intent-encoding.md` rule 3's "210 on the tree this sidecar was spliced into"** is now 228 by its own stated formula (`2 x 114`). Not filed: the rule states itself as the formula times a command and labels the figure by the tree it was measured on.

## Result

Three acceptance shortfalls, all `low`, and all in plan-record currency rather than in the enforcement design or the scheduled work. Both pass-8 dispositions are closed and reproduce independently, the dismissed Q-86 sample is byte-unchanged, and all four pass-7 dispositions R7-1 through R7-4 still hold under fresh measurement, including the `43/34/26/17` selector snapshot and Q-85's exact receipt route.

ON THE BRIEF'S CLOSURE QUESTION: a later planner or explorer CAN execute the handoff from durable artefacts alone. The Q-86 design pass has a tracked brief whose contract is internally consistent and whose obligations are selectable from the committed log; the Q-79 umbrella pass carries seven measured specification defects and positional evidence that reproduces row for row; `sidecar-status-authored-openings` is selector-defined with no hard-coded count; the Q-58 experiment's frozen-protocol contract matches its retained design field for field; and the four typed-workflow successors carry a single declaration/reconstruction authority with explicit `blocked_by` edges. The three findings are stale sentences a reader would trip over on the way, not gaps in what the handoff instructs. Criteria 33, 35, 36, 37 and 39 re-derive exactly from the committed tree and log, every gate passes, and the eighth appended acceptance record is a second live confirmation that criterion 36's bounded segment survives an appended ordinal. No finding is dismissed and none is accepted as residual, so no high or critical dismissal re-check is owed by this reviewer.

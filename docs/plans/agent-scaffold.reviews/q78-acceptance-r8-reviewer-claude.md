# Q-78 acceptance pass 8, fresh-adopter and executable-handoff lens (Claude)

Reviewed artifact: branch `review/q78-acceptance-r8-claude` at `5450323f`, whose product content is `plan/q78-design-pass` at `98b0f4d7` (the pass-7 planner repair) plus this pass's own brief commit. Baseline `main` at merge-base `7f7039d7`. 105 changed files, all under `docs/` (`git diff --name-only main HEAD | grep -v '^docs/'` returns nothing), so the product under acceptance is the planning artefact, not code. Lens: read the product first, then the ledger and the acceptance triages; ask whether a fresh planner, implementer or Q-86 explorer can execute what the plan now says from durable artefacts alone.

## Verification actually performed

Toolchain built from this worktree: `cargo` and `rustc` 1.95.0 from `/nix/store/9l9lclxjw8ns5q4k13lxld7pl90paa3g-rust-mixed/bin`, with `cc` supplied by `/nix/store/adcz0m6qq2flmshdf0zz2xwjr5zbq1gr-gcc-wrapper-15.3.0/bin` (bare `cargo` fails with `linker 'cc' not found`). The binary under test is HEAD's own (`agent-flow 0.0.4`), compiled in this worktree in the same session that produced the passing suite, so no parser or checker result below is adopted on trust. Build output went to the gitignored `/target/`, which `git status --porcelain` therefore reports as clean; every copy, mutation and probe stayed inside the authorised `TMPDIR` scratch mount, and no bare `/tmp` path was used. `git worktree list` reports all four worktrees intact at the end of this review (`agent-scaffold`, `q78-acceptance-r8-claude`, `q78-acceptance-r8-gpt`, `q78-planner`); nothing was pruned or removed.

- `cargo test`: exit 0. 470 passed, 0 failed, 0 ignored across 12 suites.
- `cargo clippy --all-targets -- -D warnings`: exit 0, zero warnings.
- `agent-flow validate --source docs/plans/agent-scaffold.plan.toml`: exit 0, `474 records, valid`, `114 steps, 87 questions, valid`.
- `agent-flow validate --source docs/plans/agent-scaffold.plan.toml --workflow --workflow-spec .agents/workflow.toml`: exit 0, `workflow invariants hold`.
- `agent-flow render --check --strict docs/plans/agent-scaffold.plan.toml`: exit 0, `up to date`.
- `agent-flow checks` through a scratch-only `PATH` shim: `1 passed, 0 failed, 0 skipped`.
- `git diff --check` and `git diff --check main HEAD`: exit 0.
- ASCII: `LC_ALL=C grep -cP '[^\t\x20-\x7e]'` sums to 0 over all 105 files changed since the merge-base.

FRESH-SCAFFOLD CRITERIA HOLD (criteria 1 to 6, 26). `scaffold --output-dir <scratch> --write --principles default --vcs none` writes 31 files; the dropped `docs/plans/TEMPLATE.plan.toml` validates and `render --check --strict`es clean; the only `w4_baseline` occurrence is the explanatory comment, so no baseline is declared; and a default re-run refreshes 18 reference assets while leaving all 13 working-ownership assets untouched, including a local edit appended to `AGENTS.md`.

## The four pass-7 dispositions: all four CLOSED and reproduced independently

### R7-1 (`medium`, the Q-86 brief made the known-unbounded baseline satisfy a bounded-path proof): CLOSED

`Q-86-convergence-mechanism-brief.md:23` now reads "These two paths make the current mechanism a required comparison baseline but not an admissible bounded candidate ... The baseline is not recommendation-eligible", and the four downstream obligations are re-scoped consistently: `:87` requires "the current mechanism as a required, explicitly non-admissible comparison baseline, plus more than one viable bounded mechanism" and forbids counting the baseline toward that total; `:89` selects the recommendation "only from the viable bounded mechanisms"; `:91` applies the finite-bound or exact-terminal-event proof "for each viable bounded mechanism" and directs the baseline to "demonstrate and report the repeated-resume and later-acceptance counterexamples" instead; `:93` presents only the viable bounded options to the human; and `:97` requires the baseline replay to "exhibit the unbounded repeated-resume and later-acceptance paths". I could not construct a proposal that satisfies `:87` and fails `:91`, which was the pass-7 contradiction. The same boundary is carried in the `[[question]]` Q-86 ask and in `docs/plans/agent-scaffold.questions/Q-86.md`, and Q-86 is still `exploring` with no `folded_into` and no receipt.

### R7-2 (`medium`, a newly authored status label on `workflow-calibration`): CLOSED

`docs/plans/agent-scaffold.steps/workflow-calibration.md:3` now opens "The scaffolded workflow's round constants and the second-triager guard were introduced after a design review", token-free, with the rest of the line unchanged. The triage's "delete only the leading `In progress. `" instruction is followed exactly (`git show 98b0f4d7 -- docs/plans/agent-scaffold.steps/workflow-calibration.md` shows a single-token deletion on that line), the consumed `convergence-investigation-fold-brief.md` is untouched, and the file leaves both drift selectors: neither `anchored.txt` nor `relaxed.txt` contains `workflow-calibration` at HEAD.

### R7-3 / Q-87 (`low`, the 44/35/27/18 measurement had gone stale): CLOSED, and the new figures reproduce exactly

Running criterion 1's anchored selector (`sidecar-status-opening-drift.md:126-133`), criterion 1's relaxed selector (`:139-145`) and the H1 script transcribed verbatim from `:172-187` against HEAD prints `relaxed=43 anchored=34 strip-relaxed=26 strip-anchored=17`, which is exactly what `:21-24` and the Q-87 decision paragraph state. The 43-minus-34 difference is the nine adjectival `Deferred <noun>` openings `:26` enumerates. The 17-member handover is `escalation-exempt git-url-fetch greenfield-flake include-all-visible instrument-flag later-enhancements mutation optional-modules reviewer-reproducible-evidence state-schema structured-skeleton task-entry-regrounding test-driven tui-authoring waiver-model workflow-audit-followups workflow-viz`.

THE HISTORY-VERSUS-CURRENT SPLIT THE TRIAGE REQUIRED IS HELD EVERYWHERE I COULD FIND IT. `agent-scaffold.plan.toml:2537` now says "At the time of the Q-84 decision, the commands reproduced 44 ... 18" and calls that "dated historical evidence"; `sidecar-status-opening-drift.md:19` heads the block "WITH Q-84 PRESERVED AS HISTORY AND Q-87 RECORDING THE CURRENT SNAPSHOT"; `:28` records both membership movements separately (`workflow-driver` 19 to 18, `workflow-calibration` 18 to 17); `:119` reads "SNAPSHOTTED AT 17 BY Q-87 BUT ALWAYS SELECTOR-DEFINED"; and the ledger's pass-7 paragraph records the same. A `grep` for every surviving `18`/`19` reference in the live plan and ledger returns only historical framings and option labels. The Q-87 receipt is record 474, `chosen` is a member of `options`, and Q-87 is `decided -> folded into sidecar-status-opening-drift` with `receipt = "Q-87"`; the owning step's provenance is `decisions = ["Q-84", "Q-87"]`, so Q-84 was not dropped.

### R7-4 (`low`, no Q-85 provenance or exact receipt on `workflow-calibration`): CLOSED

The `[[step]]` block now carries `[step.provenance] decisions = ["Q-85"]`, and `docs/plans/agent-scaffold.md:220` renders `| workflow-calibration | in progress | why: decisions Q-85 |` where pass 7 measured an empty Notes cell. `workflow-calibration.md:18` names the exact receipt as "the append-only record at `docs/metrics/workflow.jsonl:472`" and supplies a commit-independent selector; `sed -n '472p'` is the Q-85 decision object, and the quoted `jq` selector returns exactly one line. Because the log is append-only, that ordinal is stable: the two records appended since (473, 474) sit after it.

## Criteria I re-derived rather than read

- CRITERION 37's FIXTURE PARTITION REPRODUCES EXACTLY, using the tool's own join (a round's step is its structured `step` id else `task` with a trailing `-inc<alnum>` stripped; its increment is its structured `increment` id else the full `task`). 99 `plan_review` records; 63 Roadmap-resolving over exactly 18 identities; 36 non-Roadmap over exactly 14 task identities. The 63 partition into seven exact-step records (`checks-runner-worktree-name-collision` 4, `structured-skeleton` 3), 41 Q-81 historical increment records, and 15 on the three live plan-review increment declarations (`decision-folder-currency-fold` 5, `workflow-enforcement-tier-fold` 5, `workflow-enforcement-tier-endproperty-fold` 5).
- THE Q-81 ORDINAL MAP AT `structured-risk-class-source.md:28` REPRODUCES ROW FOR ROW. Records 387 to 430 hold exactly 41 rounds over the thirteen listed ids with the listed ordinals, plus the three escalations at 421, 422 and 429 and nothing else; all 41 are `plan_review` and all 41 snapshot `risky`.
- ALL FIFTEEN DIGEST PINS REPRODUCE. `sed -n "${n}p" docs/metrics/workflow.jsonl | tr -d '\n' | sha256sum` matches `:34` for records 98, 99, 115, 217, 219, 220 and 221 and `:38` for records 1, 47, 48, 164, 173, 177, 178 and 379. The cap-adoption digest at `review-loop-foreclosure-enforcement.md:21` also matches record 386 (`4e7aed64e77ca228347de1cfbcf620ca2d6b6f3d8550b606212b3b448d0e95c6`).
- CRITERION 38's BOUNDED SEGMENT HOLDS UNDER THE APPENDED ORDINAL. `task == "q78-design-pass" && phase == "plan_review"` selects exactly records 380 to 386 and no escalation record exists for that task; `phase == "acceptance"` now selects seven records (432 to 437 and 473), every one outside the `through_record = 386` window. This is the property the criterion promised would survive "regardless of appended ordinal", and the seventh acceptance record is the first real test of it.
- CRITERION 39's SOURCE GRAMMAR IS EXECUTABLE AS WRITTEN. R2's prose arm (`step-intent-encoding.md:675-678`) admits only `docs/plans/agent-scaffold.md` and `docs/plans/agent-scaffold.steps/<slug>.md` and reports anything else as `SOURCE NOT THIS STEP`; the bare metrics path is rejected with `RECEIPT LINE REQUIRED`; the receipt arm requires a schema-valid `type:"decision"` object whose `task` equals the row's slug and exposes only `chosen`/`recommendation`/`options`. The four-item residual set has one home at `:1073`, and the exploration and the `[[question]]` both point at it rather than copying it. The paragraph-valued recommendation agrees across `Q-78.md` (decision 26), the exploration's section 4 and `step-intent-encoding.md` rules 1 and 2.
- THE Q-78 ASK'S RE-DERIVED MEASUREMENTS STILL HOLD. `src/plan/source.rs:129` is `pub(crate) struct Step`, whose nine keys are the nine the ask lists; `:240` is `pub(crate) struct Provenance`; `src/plan/render.rs:180` is the sidecar-by-filename load and `:573` is `step_details_section`; `agent-flow --help` lists exactly the seven named subcommands; and `awk 'NF && $0 ~ /^#/ {print NR; exit}' docs/plans/agent-scaffold.steps/core-assets.md` prints 9 with a non-heading first line.
- CRITERION 35's PROVENANCE IS BYTE-EXACT. Extracting commit `3ad7b2b`'s plan, ledger and log into scratch and running the design's own command reproduces `1434 113409 727754`, exactly the figures at `Q-58-ablation-design.md:28`. Section 4 of that design enumerates every frozen field `q58-output-ablation.md:8` says it requires, and the plan graph enforces the ordering the criterion states: `resume-state-currency-signal` is `blocked_by = ["q58-output-ablation"]` and `workflow-driver-typed-fleet` is blocked by that plus `workflow-loop-visibility` and `review-loop-foreclosure-enforcement`.
- EVERY `file:line` CITATION IN THE BRANCH'S CHANGED PLAN SOURCES RESOLVES. 132 distinct `<path>:<line>` citations across the 33 changed step, question, exploration, TOML and criteria files; all name an existing file and an in-range line. Spot-checking the load-bearing ones, `docs/plans/agent-scaffold.documentation-protocol.md:5` and its render at `docs/plans/agent-scaffold.md:26` are the "do not repeat the status label" sentence, and `agent-scaffold.ledger.md:46` is round 2 finding G1.

## Shortfalls

### A. `low` - the Q-86 handoff brief states an acceptance count and an adjudication list that its own command contradicts on this branch

`docs/plans/workflow-calibration.explorations/Q-86-convergence-mechanism-brief.md:27` reads "The six recorded Q-78 acceptance passes are the local demonstration of the uncapped branch", and `:33` reads "At this fold the selector returns six passes and `[7,10,5,6,5,5]` ... The corresponding adjudications are `docs/plans/agent-scaffold.reviews/q78-acceptance-triage.md` and `q78-acceptance-r2-triage.md` through `q78-acceptance-r6-triage.md`".

MEASURED AT HEAD, the brief's own selector at `:30` returns seven, not six:

```
jq -s 'map(select(.type == "round" and .task == "q78-design-pass" and .phase == "acceptance")) | {passes: length, valid_shortfalls: map(.valid_findings)}' docs/metrics/workflow.jsonl
```

prints `{"passes": 7, "valid_shortfalls": [7,10,5,6,5,5,4]}`. `docs/plans/agent-scaffold.reviews/q78-acceptance-r7-triage.md` also exists on this branch and is not in the brief's list.

THIS IS NEW EVIDENCE, NOT A RE-RAISE. The pass-7 Claude reviewer verified at `ecc342e5` that "`:28`'s command returns `{"passes": 6, "valid_shortfalls": [7,10,5,6,5,5]}` exactly as `:31` states" and filed nothing, which was correct on that tree. Record 473, the pass-7 acceptance round, was appended at commit `08dc3aa6`, and `git merge-base --is-ancestor 08dc3aa6 98b0f4d7` succeeds, so the changed premise was already in the log when the pass-7 repair edited five other paragraphs of this same file and did not reach these two.

WHY IT IS A SHORTFALL AND NOT A DATED NOTE. The count half is self-limiting: `:27` directs the reader to "Derive their current count and valid-shortfall sequence from the append-only metrics log rather than copying a permanent total", and `:33` names the selector as "the authority if the log grows", so an explorer who follows the instruction gets seven. The adjudication half has no such escape. It is a hard-coded list with no selector, the brief tells the explorer to "use them to test proposed lineage, scope, and severity rules rather than inferring those rules from counts alone", and `q78-acceptance-r7-triage.md` is the most recent adjudication and the one whose findings (an unbounded-baseline design contract and a stale decision premise) bear most directly on the mechanism question the explorer is being asked to answer. So a later explorer working only from this brief tests its lineage and severity rules against six of the seven available adjudications and never learns that a seventh exists. The branch itself sets up that omission, because the brief is the durable handoff the Q-85 fold created and criterion 41 is what schedules it.

It is `low` rather than `medium` because the evidence is recoverable (`ls docs/plans/agent-scaffold.reviews/*-triage.md` finds the file), because the count clause carries its own correction rule, and because nothing in the design contract that R7-1 repaired depends on the number being six.

SMALLEST SAFE DISPOSITION. Give the adjudication list the same treatment `review-loop-foreclosure-enforcement.md:19` already gives the acceptance sequence, which is the branch's own worked example of the durable form: "This selector remains durable as acceptance passes append; no acceptance ordinal is part of the contract." State the adjudication set as a path glob rather than an enumeration, and re-date or de-count `:27` and `:33` so no figure in the brief is stated as current without its own re-derivation command. Do not write a pass count into a Success Criterion.

### B. `low` - the step that just gained the Q-86 design pass claims its explorations directory holds three files when it holds thirteen, and the pass-7 repair deleted the plan's only pointer to that staleness

`docs/plans/agent-scaffold.steps/workflow-calibration.md:9`, rendered verbatim into `docs/plans/agent-scaffold.md:708`, reads: "The directory currently holds `2026-08-13-audit-when-the-loop-turned.md` (the verdict, its evidence, and ten ordered recommendations), `2026-08-13-audit-measurement-methods.md` (the methods reference), and `calibration-analysis.md` (the 2026-07-31 prior analysis ...)".

MEASURED AT HEAD, `git ls-files docs/plans/workflow-calibration.explorations/` returns thirteen tracked files. The ten the sentence omits are `2026-08-14-causation-investigation.md`, `Q-86-convergence-mechanism-brief.md`, `finding-provenance-a.tsv`, `finding-provenance-b.tsv`, `finding-provenance-extract-a.md`, `finding-provenance-extract-b.md`, `fix-pass-shape-a.tsv`, `fix-pass-shape-b.tsv`, `fix-pass-shape-mechanical-a.md` and `fix-pass-shape-mechanical-b.md`.

THE BRANCH BOTH WORSENED THE CLAIM AND REMOVED ITS ROUTING RECORD, which is what makes this a pass-8 shortfall rather than inherited noise. `Q-86-convergence-mechanism-brief.md` was added to that directory by this branch at commit `a73b3659`, so the Q-85/Q-86 fold put a thirteenth file into a directory whose own step sidecar enumerates three, then edited two paragraphs lower in that same file (`:18` and `:20`) without touching `:9`. Separately, the pass-7 repair recognised this exact class one file over: `git show 98b0f4d7 -- docs/plans/agent-scaffold.steps/sidecar-status-opening-drift.md` changed "`docs/plans/workflow-calibration.explorations/` holds 12 files, and ... two `type:"decision"` receipts" to "holds the audit and related design records, and ... decision receipts ... including `Q-73`, `Q-76-causation`, and Q-85", de-counting the parallel claim there. In the same commit it rewrote the NOT-IN-SCOPE bullet at `sidecar-status-opening-drift.md:115`, dropping the sentence that had named this defect and routed it. At `98b0f4d7~1` that bullet read "In particular, `workflow-calibration.md`'s stale audit-record inventory is not edited here ... A later currency pass or the successor step's planner may scope that sentence explicitly"; at HEAD it says only that the file "is now outside both dynamic drift worklists". So the branch removed the plan's one live pointer to a stale claim it had just made staler, and no live plan text now names it.

THE MEASURED CONSEQUENCE, WHICH IS THE FRESH-EXPLORER ONE. `2026-08-14-causation-investigation.md` is a 35 KB Markdown design record produced by this step (its `Q-76-causation` receipt carries `task` `workflow-calibration`) and it falsifies a causation hypothesis about when the review loop turned, which is squarely the subject Q-86 is asked to settle. `grep -rn '2026-08-14-causation-investigation' --include='*.md' --include='*.toml' .` outside the transient reviews directory returns exactly one hit, `docs/plans/agent-scaffold.ledger.md:733`. It is named nowhere in the plan and nowhere in the Q-86 brief, whose "Existing calibration work" section at `:37` directs the explorer to `calibration-analysis.md` and `2026-08-13-audit-when-the-loop-turned.md` only. Since the ledger is deleted at task close and the plan is the durable artefact, an explorer entering this step from the plan is told the directory holds three files and is given no route to the one record that most directly tests the hypothesis the design pass will re-open.

WHAT KEEPS IT AT `low`. The sentence was already incomplete before this branch (ten of the thirteen files predate `main`), so the branch inherited most of the gap rather than creating it; no Success Criterion states a file count; the correct list is one `ls` away; and the defect is in a planning record rather than in shipped guidance. It is a shortfall rather than a nit because AGENTS.md phase 5 makes "does the shipped change leave any doc or prompt stale?" an acceptance duty whose answer routes back to implementation, and because the branch's own fold is what added the thirteenth file.

SMALLEST SAFE DISPOSITION. Replace the three-file enumeration at `workflow-calibration.md:9` with the same de-counted form the pass-7 repair already chose for the parallel claim, naming the directory and the records that carry an argument rather than asserting a complete inventory, and add `2026-08-14-causation-investigation.md` to the Q-86 brief's "Existing calibration work" reading list so the falsified causation hypothesis reaches the explorer. Do not write a file count anywhere.

## Recorded but not filed

- **The exploration's `45`/`21` status-opening figures are superseded.** `docs/plans/step-intent-encoding.explorations/Q-78.md:45` and its duty (g) answer state "45 step sidecars open ... with a Roadmap status token, and 21 of those 45 name a state their step is not in". The relaxed selector now reaches 43, and `sidecar-status-opening-drift.md:62` records that two of the 21 (`workflow-driver`, `workflow-calibration`) have since received token-free openings. Not filed: the exploration's own preamble states that "SEVERAL FIGURES HERE DO GROW, including the status-opening population in section 2", names `sidecar-status-opening-drift` as carrying "the selecting command and the enumeration of the 21 by slug", and `step-intent-encoding.md`'s rule 9 already retires the 45 explicitly ("AN EARLIER FORM STATED 45"). Criterion 39 binds the exploration only on the recommendation, the residual pointer and the decision selectors, all three of which agree.
- **`next` projects `workflow-calibration` as `awaiting-first-review` and emits 736349 bytes.** Measured with the HEAD binary. Not filed on the same ground pass 7 recorded: these are exactly the two limits Q-82 and the reopened Q-58 schedule, and criterion 35 records the byte totals as evidence and never as a target.
- **Criterion 5 of `structured-risk-class-source` still hard-codes the 15 and 36 halves of a live population.** Pass 7 recorded this and did not file it; I have no new evidence, because no `plan_review` record has been appended since record 430 and every figure still reproduces exactly.
- **Ledger obligation items (3) and (4) remain unregistered**, and item (5) now reads falsely (`Q-80` is `decided -> folded into review-loop-class-inheritance`, not `open`). Not filed: the paragraph heads itself "HISTORICAL PRE-ACCEPTANCE OBLIGATION LIST, RETAINED AS EVIDENCE AND NOT AS CURRENT INSTRUCTION", which dates all three by label, and neither (3) nor (4) maps to a Success Criterion. This is the third pass to note the same paragraph's closing sentence routing items through the acceptance planner repair while its heading disclaims being current instruction.
- **Twenty-two `[[step]].title` values differ from their sidecar's own `### <slug>:` heading**, including `workflow-loop-visibility`, which this branch added. Not filed: no plan rule requires the pair to match, most differences are deliberate abbreviations, and `sidecar-status-opening-drift.md:34` already names the pair as an unchecked hand-maintained duplication belonging "one level up" from that step.
- **`sidecar-status-authored-openings.md:35` names Q-84 but not Q-87.** Not filed: nothing it says is false, and its operative rule ("no implementation criterion hard-codes either dated count") is exactly what Q-87 confirms.

## Result

Two acceptance shortfalls, both `low`, and both in the Q-86 handoff surface rather than in the enforcement design. All four pass-7 dispositions R7-1 through R7-4 are closed and reproduce under independent measurement, including the non-admissible baseline, the token-free `workflow-calibration` opening with its selector consequence, Q-84's preserved historical snapshot beside Q-87's `43/34/26/17` current snapshot, and Q-85's exact receipt route. Criteria 35, 37, 38 and 39 re-derive exactly from the committed log and tree, and the seventh acceptance record is the first live confirmation that criterion 38's bounded segment survives an appended acceptance ordinal. Every gate passes. No finding is dismissed and none is accepted as residual, so no high or critical dismissal re-check is owed by this reviewer.

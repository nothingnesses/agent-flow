# Kickoff prompt

Copy this, add optional context, and paste it to an agent after `.agents/work.toml` names one active selected action.

---

Read `AGENTS.md` and `.agents/work.toml`, then run `agent-flow next` to confirm the selected action. Use the bounded delivery in `AGENTS.md`: one implementation branch, one implementation pass, one independent product review, conditional separate triage, at most one scoped fix, and one focused verification. Do not create planning, ledger, round-log, or review-directory state. Stop and return unresolved work rather than opening another loop.

Optional context or constraints: [add any context, or delete this line].
